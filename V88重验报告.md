# V88 新流水线重验报告（修复前视偏差）

> 日期：2026-08-17 | 数据：8-14 COMMITTED epoch（本地 kline_cache，4,905 只股票）
> 方法：outcome-free 信号重建 → 冻结 T+1 回放 → 经济门槛评估（遵守防再犯规则 R1-R7/R13-R15）

---

## 一、结论（先看结论）

**V88 无法通过新流水线生产门槛。修复其前视偏差后，真实表现为：**

| 指标 | 原 V88（历史报告） | **修复后（重验）** | 门槛（V633） |
|---|---|---|---|
| 交易数 | 532 | 13,436 | n≥1000 |
| 胜率 | **80.08%** | **41.60%** ❌ | ≥55% |
| 平均净收益 | +2.84% | +0.22% ❌ | ≥+0.5% |
| PF | 6.23 | 1.10 ❌ | ≥1.15 |
| payoff | 1.55 | 1.55 ✅ | ≥0.70 |
| 2023 年 | +74% WR | **-0.70% avg** ❌ | 每年正 |
| 2026 年 | +79% WR | **-0.21% avg** ❌ | 每年正 |
| T+1 违规 | 0 | 0 ✅ | =0 |

**V88 的"80% 胜率"几乎全部来自前视偏差**：其 TP 目标用 event 之后 20 根内的"未来高点"设定（`v81_contextual_smc_generator._future_liquidity_target`），等于"提前知道未来会涨到哪"。修复为"入场前可见的确认摆动高点"后，胜率崩塌 38pp，年度转负，PF<1.15。

---

## 二、发现的前视偏差（铁证）

**v81_contextual_smc_generator.py L156-161**：
```python
def _future_liquidity_target(ks, event_idx, min_price, lookahead=20):
    for j in range(event_idx + 1, min(len(ks), event_idx + lookahead + 1)):  # ← 未来 20 根！
        future_high = _v(ks[j], 'h')
        if future_high > min_price:
            return future_high
```
- 被 `locate_poi`（L202）调用 → 每个候选的 `liquidity_target`（→ TP1/TP2/TP3）**都用了 event 后 20 根的未收盘高点**
- V90 文档早有警告："Do NOT inherit V86 future-bar liquidity"（v90_daily_full_market_scanner.py 头注）—— 但 V88 契约层从未修复
- **这就是 V88 高胜率的根源**：TP 设在"未来一定会到的最高点"上

---

## 三、重验方法（修复 + 冻结回放）

### 3.1 信号层（outcome-free）
复制 V81/V85 信号语义（BOS 事件 → 需求 OB 回踩 → touch/reclaim → 次日开盘入场），**修复 TP**：
- 旧：TP = 未来 20 根内最高点（前视）
- **新：TP = 入场前最近一个已确认的摆动高点（swing high，3/3 确认），必须高于入场前可见的 event 高点/zone 上沿；找不到则丢弃该候选（R2）**
- 其它检测只用 entry 前数据（R13）

### 3.2 冻结回放（R3/R4）
- 入场 = entry_date 开盘价（R1：可成交）
- SL = zone_low × 0.99（结构止损，entry 前可见）
- TP = 修复后的摆动高点（entry 前可见）
- T+1：出场从 entry 后第一根 bar 开始；SL 优先碰撞；跳空按开盘价（GAP_SL）
- 费用 0.20% 往返；max_hold=40；单标的不限重叠（身份按日期去重）

### 3.3 参数（R5 预注册）
pivot 3/3、LOOKBACK=5、MAX_WAIT=5、zone_width∈(1.0,1.6]、SL_BUFFER=0.99、MAX_HOLD=40、FEE=0.20%

---

## 四、重验结果（13,436 笔，4,461 只股票）

| 年 | n | WR% | avg净% | cum% | avgWin% | avgLoss% | payoff | PF | 持仓K |
|---|---|---|---|---|---|---|---|---|---|
| **总体** | 13,436 | **41.60** | **+0.22** | +2,896 | +5.49 | -3.54 | 1.55 | **1.10** | 5.3 |
| 2023 | 1,593 | 32.83 | **-0.70** | -1,117 | +4.92 | -3.45 | 1.43 | 0.70 | 5.2 |
| 2024 | 3,479 | 36.79 | +0.21 | +718 | +6.71 | -3.58 | 1.88 | 1.09 | 5.0 |
| 2025 | 6,192 | 47.09 | +0.61 | +3,762 | +5.21 | -3.49 | 1.49 | 1.33 | 5.7 |
| 2026 | 2,169 | 40.11 | **-0.21** | -466 | +5.00 | -3.71 | 1.35 | 0.90 | 4.7 |

- 月频：仅 3 个月 n≤4（202303/202305/202306），月度门槛基本满足
- T+1 违规：0
- 年度：**2023、2026 平均净收益为负**（不满足"每年正"）

---

## 五、独立 Oracle 验证（R14）

- 用独立代码路径（不同函数结构、同语义参数）重算核心段身份集合
- **交集 29,295 / seed 44,666（66%）**；差异来源：seed 含 V85 扩展段（_expanded_bos_event 多 lookback + MIXED 分支 + takeover 检查），oracle 只复刻核心 BOS→POI→reclaim 段
- **结论**：seed 核心逻辑可独立复现、确定性成立（非随机/状态泄漏）；完全一致需复刻 V85 全部扩展逻辑（覆盖范围差异，非缺陷）

---

## 六、对系统现状的解释（回应用户核心疑问）

1. **"为什么生产是 V88 而不是 V517"** —— V88 从未被新标准检验；重验证明它**过不了新标准**（前视偏差拿掉后 WR 41.6%）
2. **"为什么 700+ 次迭代没有一个取代 V88"** —— 因为 V88 的高指标是虚的（前视偏差），**没有任何版本（包括 V88）在真实标准下通过**；V699（WR 53.3%/PF 1.38）反而比修复后的 V88 更真实
3. **"EMPTY_BOOK 是否正确"** —— **正确**。系统在严格标准下确实没有可生产的策略；V88 的"生产"是展示层残留
4. **迭代方向**：V88 信号族（BOS 回踩）修复后仍是 regime 依赖（2025 强、2023/2026 负）—— 与 V519/V699 同构。**该信息族在 A 股跨年无稳定 edge 是系统性的**；突破只能靠新因果维度（V633 Lane A-D）

---

## 七、合规检查（防再犯规则清单）

| 规则 | 状态 |
|---|---|
| R1 入场价可成交（开盘价 ∈ [low,high]） | ✅ 入场用次日开盘价，构造上可成交 |
| R2 目标入场前可见 | ✅ 用入场前已确认摆动高点，无未来数据 |
| R3 严格 T+1 | ✅ same-bar 冲突 SL 优先，T+1=0 |
| R4 费用 0.20% + GAP_SL | ✅ |
| R5 预注册参数单次回放 | ✅ |
| R6/R7 经济门槛 | ✅ 已按 V633 门槛评估（未通过） |
| R13 outcome-blind seed | ✅ 只读 entry 前数据 |
| R14 独立 oracle | ⚠️ 核心段 66% 独立复现（V85 扩展段未复刻） |
| R15 身份用日期 | ✅ symbol\|entry_date\|event_date |

## 附：产物
- `E:\test\smc_project\smc_backtest_report\V88_reverify\v88_reverify_trades.csv`（13,436 笔全量）
- `v88_reverify_seeds.csv`（44,666 seeds）
- `v88_reverify_oracle_report.json`
- 脚本：`v88_reverify.py`、`v88_oracle.py`
