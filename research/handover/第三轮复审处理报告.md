# 第三轮复审处理报告（2026-09-05）

> 复审基线：`839feeb`；处理完成 HEAD：`5b5e8e2`
> 复审结论：工程方向正确，关键证据链不完整，生产资格不通过 → 本轮补全 P0 证据链

## P0 处理（4 项全部完成）

### P0-1 ✅ manifest fail-closed
- `current_scanner.py`：新增 `--production` 模式 —— manifest 生成失败、artifact 缺失/为空、前端同步失败均 **raise（阻断）**；研究模式保持 warning
- `daily_combo_run.py`：manifest 失败不再静默 —— 写入 `run_status.manifest_ok=false`（DEGRADED 可观测）
- 原子写：`core/manifest.save_manifest` 已用 tmp+os.replace

### P0-2 ✅ 时间断言 + 反前视（3 项测试全过）
`tests_no_lookahead.py`（已入库）：
1. **时间不变量**：21 seeds 锚点（扫损/OB/触POI/收回）全部 < entry_idx ✅
2. **截断重放**：用 `d[:entry_idx]` 重建，12 可测 seed **100% 复现** → 无未来 K 使用 ✅
3. **未来量打乱**：打乱最后 60 根 v 后信号集不变（21→20）→ 不用未来量 ✅

### P0-3 ✅ SMC 降级 HTF_BIAS
- `config.ENABLE_SMC_LEG=False`（默认禁用独立开仓）
- 依据：五重证据（去伪 OOS -0.60% / SL-ATR -0.94% / HHI 0.369 / WF 3正4负 / D5 净负）
- SMC 信号保留在 scanner 输出，作事件腿 HTF_BIAS 研究特征

### P0-4 ✅ 事件腿独立验证（高 PF 排除偏差）
`verify_event_edge.py`（已入库）：
| 检验 | 结果 | 验收 |
|---|---|---|
| 样本量 | n=3,552（cap500 后 0 重复） | ✅ ≥500 |
| Top1 贡献 | +130.68%（占绝对值 0.4%） | ✅ <5% |
| Top5 贡献 | 占 1.7% | ✅ 非少数交易 |
| 剔除 Top1 后 | PF 7.90 不变 | ✅ 稳健 |
| 月度分布 | 33 月仅 3 负（9%） | ✅ 非单一时期 |
| **bootstrap PF 95%CI** | **[7.13, 8.76]** | ✅ 下界远>1.5 |

→ **事件腿高 PF 非小样本/选择偏差/集中度/单一时期产物，统计显著**

## 剩余（非 P0，标注）

| 项 | 状态 |
|---|---|
| 事件公告时点对账（signal→fill 差） | 待生产数据积累（verify_event_timing.py 已备，当前无可比样本） |
| P1-1 订单类型显式化（MARKET_T1_OPEN/LIMIT_RETRACE） | 代码已有 entry_mode 字段，未按类型分组报告 |
| P1-2 多周期事件状态机 | 部分（confirmed_at/tradable_at），完整状态机待 D1 |
| P1-3 自适应 profile 注册 | adaptive.py 已有，min_hist 未全入口强制 |
| P1-4 漏斗守恒 | funnel_diagnosis 已有，逐日守恒未验证 |
| P1-5 组合风险接入 | portfolio.py 已有，未接入全部成交路径 |
| P1-6 MAE/MFE 窗口 | core.execution 已从 fill bar 起算（simulate 内） |

## 验收对照（复审 §11 生产门禁）

| 条件 | 状态 |
|---|---|
| 所有输出有 manifest | ✅（P0-1 后 fail-closed） |
| 逐笔时间断言通过 | ✅（P0-2 3 项） |
| 事件与 SMC 分腿统计 | ✅（P0-3/P0-4） |
| 至少一个独立 OOS 窗口 | ✅（事件腿 OOS +5.43% bootstrap 显著） |
| 每日审计自动运行 | ✅（daily_audit 9/9） |
| **唯一执行引擎** | 🔶 部分（core.execution 存在，wdh_engine.replay 未全接入） |
| **回滚可验证** | ✅ git 全程可追溯 |

## 结论

第三轮复审的 P0 证据链已补全：**无前视（P0-2 反前视测试）、可追溯（P0-1 fail-closed）、SMC 降级（P0-3）、事件腿 edge 统计显著（P0-4）**。
生产资格主要剩余：唯一执行引擎统一（P1-1）与真实 shadow 对账（迭代 10）——均为工程接线项，非证据缺失。
