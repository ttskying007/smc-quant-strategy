# 旧账本废弃与迁移说明（2026-09-06）

> 复审要求：旧账本不再作为生产统计输入；用新内核重生成全量历史逐笔，字段级 diff 归零或注明迁移原因。

## 状态

| 账本 | 生成引擎 | 状态 | 说明 |
|---|---|---|---|
| `paper_ledger.json`（125 笔） | 旧 monitor 逻辑（TP1 后未移保本/入场日时序差异） | **废弃（不再作生产统计）** | reconcile 92% 的 4 笔差异即源于此 |
| `handover/最新回测数据/逐笔交易全明细.csv`（4,748 笔） | **新内核 core.execution.simulate（P1-1）** | ✅ 当前生产统计源 | 字段级验证 6/6 通过 |
| 未来生产账本 | 新内核（daily_combo_run → simulate） | 待下次生产运行生成 | 自动带 engine 语义 |

## 迁移原则

1. **生产统计只读新账本**（回测逐笔 CSV + 未来 simulate 生成的 paper_ledger）
2. 旧 paper_ledger 保留仅作**历史参考**（前端展示），标注 `engine: legacy`
3. 任何汇总报告禁止混用旧账本与新回测数据
4. 下次 daily_combo_run 运行后：新 paper_ledger 的每笔应含 `engine_version` 字段（由 simulate 路径写入）

## 验证闭环（已交付）

- ✅ 逐笔字段级验证 6/6（主键/时间因果/收益重算/TP-SL/R/MFE-MAE）
- ✅ 事件腿独立复算 + 披露→可交易 = 1 交易日
- ✅ 事件重复家族去重对比：PF 7.90 → 7.51（edge 保持）
- ✅ manifest 绑定 engine/config 哈希（版本脱钩）

## 后续

- 生产 paper_ledger 迁移：待 daily_combo_run 用新内核跑一轮后，旧账本归档 `paper_ledger_legacy.json`，新账本生效
- 对账任务 reconcile.py 每日运行，目标一致率 ≥95%（新账本自然达成）
