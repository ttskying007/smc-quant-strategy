# 迭代方向（2026-09-19)

**约束 （不变）**
- 所有改动走"假设 → 受控 A/B → 决策"三段；晋级需 OOS avg 与 PF 双升；
- 研究冻结期：不改生产 TP/SL； 同族参数迭代受限 （技能级要求）;
- PAPER 剩余 ≥30 closed 前生产策略一律不变动；
- 基线不变量 （冻结 2026-09-16): n=1527, avg +3.77%, PF 3.63; OOS n=336, avg +4.27%, PF 4.03。

---

## 现状归因回顾 （2026-09-19)

combo_v20f_trades.csv EVENT 腿 1527 笔 / 平均 +3.77% / PF 3.63；亏损单 530 笔、总亏 -2183.94%。

R13 20 类归因已完成并接入 PAPER 生产链。损耗主因按亏损额降序：

| 类别 | n | 亏损占比 | 含义 |
|---|---:|---:|---|
| LOSS_SL_TOO_WIDE | 203 | 49.3% | SL 距 >3×ATR，结构位过宽； 但 ATR 收紧实验显示 OOS 反降，生产保留 |
| LOSS_GAP | 112 | 16.3% | 跳空击穿 SL，执行成本，无法消除 |
| LOSS_TIME_SHORT | 59 | 10.9% | 入场后 ≤2 根直接 SL_HIT — 本轮受控延迟假设被否决，非因子 |
| LOSS_MFE_REVERSAL | 44 | 5.5% | MFE≥1R 但最终回吐 — TP 延迟/位置偏保守 |
| LOSS_HIGH_RANK+LOW_RANK | 55 | 11.4% | rank 分层无效： rank>=4 反而优于 rank<=3 (+2.02pp) — token 必须重新解释 |
| LOSS_TIME_LONG | 30 | 5.1% | 持仓 >8 bar 仍无起色 → 时间成本单一消耗源 |
| 其余 6 类汇总 | ~27 | ~2% | 兜底 |

R38ab 生产验证后现有阶段 rank>=3 的 gate online；但数据显示 high_rank 比 low_rank 显著更好—— 这意味着 持续动用 rank 作为 NC filter 还能带来增量。

---

## 五个后续方向 （按依赖 + 损耗量排序）

### 方向 1: rank 信号语义审计 (fast, 现在可做）
现状： hi/low rank 亏损占比 11.4%，但这更多是数据采样问题——市场本来高rank，rank 位置切合 event leg。
数据 ( 最近一次 live flush 1619)： **OOS rank≥4 avg+4.66/pf 4.41 vs rank≤3 avg=+2.74/pf.3.02 （差异 +1.92pp)**。
今日把 rank_audit 往"分层逻辑上 update"：硬阈≥3 应解明，将 PAPER 台账样本累积到 30 笔后再查 OOS rank 漂移是否有效。

### 方向 2: 行情兴趣表达提往 R38 行业 RS / 标的 breadth
- 과거 索引 20日 +7.45%，但 60日 +1.51% → 指数重心被权重股抬升 — 说明 flipped 拉资严重
- 同时见 RS 行为 = 行业显示最强 +28-45%，ultra-worst &pos;弱百应等
**传统 SMC 六态加一项 "industry_breadth" 打深**。
不能用一线原 result 把所有指标拼在一起，否则对样例 overfit。

### 方向 3: R14 工程小项（挂单返回概览/日历三态）
目标：report->selection_result.json-> daily_combo_run 地捆绑 `selection_result` 与 day_status；可行性极高。 先修交付激活然后清理
对应需求： 用户查看网页需要透穿 "选股里没有 stock" —直接区分 信号缺/市场数据缺失/休市。

### 方向 4: R7 上传漏斗基线（本周末将推出 ±2σ）
每天只要咱们 paper_sim 交易 ≥1 order,funnel_monitor 每天累积0，第 8 天会触发技能报警。现在仅 5 天数据。

### 方向 5: R10 已断链生产返回卡故障胶带
现有 fix 已上 GitHub,watcher=dedup index + daily fetch, 现已循环自由。下周没问题，09-20号的 daily pull 不再有 600s timeout。后续距离主要风险在（announce 基因数据长期静音 healthcheck) --如果未来 每天最后日保存 return 补救 / monitor 每天 announce。

---

## 已完成的近期防控线（我用他的回归（V20f) AB 反向拒绝）

- **延迟 1 bar 入场 A/B**: 拒绝 (OOS +3.76→+2.20; PF 3.63→2.19，子集 TIME_SHORT 恶化更糟） → 不入生产
- **SL 结构位收窄（v2_sl_controlled_ab)**: 拒绝 (OOS avg 微升 0.03pp 但 PF/WR/IS 均降） → 回滚到 B3 宽 SL
- **弱市加权 k=2**: 拒绝 （纯净 WF 8 窗 k=2 一次未选；v1.5✖权权重） → config 锁定 False

综合上述，真正的缺口很显然： **R13 已Stop原因的成本分布正在变化； 但生产侧尚不能使用这些规则** （目前没有前提修改）。

为了满足"审计→修复"→ 的迭代条件，今天的额外工作：**R13 之后应当按照我们的预签 discipline有板有眼，拒绝乱改**；在达成上述 5 条 metrics 之前 build 方法 info 优先，新的行为改动必须 exists 单独的"假设→AB→决策"层。
