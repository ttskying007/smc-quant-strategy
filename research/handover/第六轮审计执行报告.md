# 第六轮审计执行报告 —— P0 完结 + 漏斗诊断 + 安全修复

> 执行日期：2026-09-08 | 基线：`98a7b60`（五轮收官）→ 本轮 `0a3da24` + `cf4b054` 之后
> 范围：外部深度审计反馈的 P0/P1 全清单 + "无新股"漏斗诊断 + 凭据安全事件处置

---

## 一、本轮全部修复项

### P0 级（回测/实盘一致性核心）

| 项 | 修复 | 验证 |
|---|---|---|
| **P0-1 事件"公告"误杀** | 核对：现行 `NEG_WORDS` **不含"公告"**（G25 已修，反馈引用旧代码）。补充分层分类 `classify_title_detailed`：NEG_HARD（终止/取消/解除/减持/结束）与 NEG_SOFT（进展/完成/调整等），软否含金额/比例增量 → `PROGRESS_WITH_DELTA` 研究候选（默认流仍拒绝，待 A/B 验证） | tests_events 20/20 + 分层单测 |
| **P0-2 今日披露被跳过** | `daily_selection` 不再要求 T+1 K 线存在；TP/SL 基于披露日收盘；`v_ratio` 改用披露日量（原 T+1 量不可知）；`valid_from` 日历推算（周五披露→下周一） | tests_causality 18/18 |
| **P0-3 延续腿前视** | `continuation_scanner`：signal=最新收盘日(len-1)，VWAP/支撑/波动只算到 signal 日；entry=下一交易日（`next_trade_day` 日历），不写历史 entry 价 | tests_causality |
| **P0-4 统一执行核心** | **新增 `core.execution` 三函数**：`plan_order`（信号→挂单计划，不含成交价）/ `try_fill`（停牌/涨跌停(板块)/valid_from/入场模式统一撮合）/ `try_exit`（T+1锁→SL/BE→TP1部分→TP2全平→时间止损，与 simulate 逐bar判定顺序**完全一致**）。`paper_sim.realtime_monitor` 成交与离场**全部委托**，本处只管账本语义（realized_pnl/note/汇总） | tests_exec_unified 19/19；reconcile 分层统计：统一核心后 n=0 不一致=0 ✅（历史遗留 6 笔 TIME_STOP vs BE 属旧手写逻辑，账本只读不追溯改） |
| **P0-5 涨跌停板块** | `paper_sim._is_limit_up` 委托 `core.execution.is_limit_up(code)`（主板10/创业板科创20/北交所30） | tests_redteam 14/14 |
| **P0-8 凭据安全** | `test_twikit_*.py` 明文密码 → 环境变量读取；**git 全历史重写**（filter-repo replace-text）清除；强制推送；README 安全公告（密码已轮换作废） | 本地密钥扫描工作区 0 hits + 全历史 0 hits |

### P1 级

| 项 | 修复 |
|---|---|
| **P1-1 追踪止损失效** | `stop = max(ep, last_sl) if be_active else last_sl` —— 结构追踪真正生效（tests_execution 第9组构造K线验证：TP1后 trail>ep 时跳空低开触发 SL_GAP 而非 TIME_STOP） |
| **P1-2 TP3/4 死码** | 删除不可达矛盾分支，统一两段式 TP1(30%)+TP2(70%全平) |
| **P1-3 run_status 分层** | data/signal/execution/frontend 四层 + `production_eligible` |
| **PENDING 过期（反馈第12点）** | valid_from + 3 交易日未成交 → EXPIRED（防长期 pending 阻塞同代码新信号），`PENDING_EXPIRE_DAYS` 入 config |
| **requirements.txt** | 依赖锁（核心零第三方依赖，标准库 only） |
| **GitHub Actions CI** | 语法 lint + 6 个纯逻辑测试套件（可移植，外部目录验证通过）+ 密钥扫描（工作区+全历史） |

## 二、"无新股"漏斗诊断（方向7 落地）

新增 `selection_funnel.json`（每日自动生成，前端镜像同步）。**首个诊断结果（近 5 交易日 2026-09-02~09-07）**：

```
公告总数(含增持/回购关键词): 153
├─ 分类为正事件:            22   (14.4%)
│   ├─ dup 去重:            -6
│   ├─ 无K线:              -2
│   ├─ 阶段(ACCUM/DOWNTREND): -9
│   ├─ ADX<20:            -5
│   └─ 挂单:               0（当日 dup 完）
├─ 硬否(终止/取消/解除/减持/结束):  5
├─ 软否(进展/完成/调整/结果等):     123  (80.4%)
│   └─ 其中含金额/比例增量(研究候选): 3
└─ 结论: 瓶颈 = 事件过滤软否词(123/153=80%)，非市场无机会
```

**关键发现**：约 80% 的增持/回购公告被软否定词（进展/完成/调整/结果）过滤。这正是反馈预测的"过滤过严"——但**解药不是立即放开**（进展公告多为已兑现信息，需 A/B 验证），而是：
1. 分层分类已就位（`PROGRESS_WITH_DELTA`=3 笔已识别为研究候选）
2. 下一步：对 3 笔含增量进展公告做 T+1 入场回测 A/B（保留 vs 拒绝），OOS 验证后再放开

## 三、测试体系（最终状态）

```
13 套件 173 PASS / 0 FAIL
├─ tests_execution      17（+P1-1 追踪止损 2）
├─ tests_exec_unified   19（新增：P0-4 三函数全路径）
├─ tests_manifest       10
├─ tests_events         20
├─ tests_portfolio      11
├─ tests_no_lookahead    3
├─ tests_coverage       13
├─ tests_redteam        14
├─ tests_audit_core     19
├─ tests_audit_f5_f20    7
├─ tests_adaptive       12
├─ tests_ledger_types   10
└─ tests_causality      18（P0-2/P0-3 因果时序）
```

CI 可移植套件（无本机数据依赖）：tests_execution / tests_exec_unified / tests_events / tests_manifest / tests_portfolio / tests_causality = 95 项，GitHub Actions 每次推送自动跑。

## 四、遗留与不修项（诚实记录）

1. **reconcile 历史差异 6 笔**（TIME_STOP vs BE/SL_GAP，2026-08-12~08-19）：统一核心**之前**的旧手写逻辑产物。账本只读不追溯改（改写历史账本违反审计原则）；统一核心后新离场 0 不一致。9/8 后随新交易积累，新旧占比自然替换。
2. **SMC 腿维持禁用**：五重证据（去伪/SL-ATR/HHI/WF/D5）结论未变，本轮无新 OOS 证据不重启。
3. **60m 层维持关闭**：loose/strict 双定义实证均负（−3.96pp/−2.30pp）。
4. **软否词放开**：待 PROGRESS_WITH_DELTA A/B 回测验证（下一步研究项，非代码项）。
5. **反馈中的状态机/打分制/自适应参数/多周期重做**：属"方向3/4/5"架构级重构，本轮先完成其地基（因果时序+统一执行+漏斗），重构本身需独立立项（建议下一轮）。

## 五、git 链

```
本轮: cf4b054 安全修复(历史重写+凭据轮换) → [历史全重写, 旧哈希作废]
     02e645a AI助手MVP(重写后)
     694aee5 AI设计方案(重写后)
     0a3da24 P0/P1审计修复(P1-1/P0-2/P0-3/P0-5/P1-2/P1-3)
     ← 本报告 + P0-4统一执行三函数 + PENDING过期 + 漏斗 + CI (即将提交)
```

> 注：2026-09-08 凭据安全事件导致全历史重写（54 提交），所有旧 commit 哈希已失效；
> GitHub 已强制推送替换。审计文档中的旧哈希引用以时间线为准，不再对应仓库。