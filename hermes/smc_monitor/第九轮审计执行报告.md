# 第九轮审计执行报告 —— P0/P1 全面收口

> 执行日期：2026-09-08 | 依据：外部复审报告（P0-1~P0-4 / P1-1~P1-6 / 生产门禁）
> 状态：全部 P0 + P1 项已实现并通过测试验证

---

## 一、P0-1：manifest fail-closed（✅ 完成）

**审计要求**：manifest 失败必须阻断生产（fail-closed）；artifact 缺失/空/哈希不符 → INVALID；原子写；前端同步失败 → DEGRADED。

**实现**（`core/manifest.py` + `daily_combo_run.py`）：
- `validate_artifacts()`：artifact 不存在/为空/哈希重算不符 → `status=INVALID`
- `finalize_manifest()`：验证 → 原子写(`os.replace`) → 写后复核 → 失败抛 `RuntimeError("production run blocked")`
- `daily_combo_run`：`_production_eligible = 四层完整 AND _manifest_ok`（manifest 失败即使数据完整也禁止生产资格）；前端同步失败仍降级记录

**测试**：`tests_manifest` 17/17（+5：缺失→INVALID / 空→失败 / 篡改→哈希不符 / finalize 抛阻断）

## 二、P0-2/P1-1：时间因果硬断言 + 订单类型显式化（✅ 完成）

**审计要求**：证明字段进入唯一撮合路径；订单类型显式化（MARKET_T1_OPEN / LIMIT_RETRACE）；每笔记录 submitted_at/eligible_at/fill_rule/fill_price/not_filled_reason。

**实现**：
- `core/execution.py try_fill`：返回 `fill_rule`（LIMIT_RETRACE:px<=ref / open_fallback / MARKET_T1_OPEN）+ `price_source`（retrace/open）
- `paper_sim.py`：选股记录 `order_type`/`submitted_at`/`eligible_at`/`fill_rule`/`not_filled_reason`（LIMIT_RETRACE 与 MARKET_T1_OPEN 两类显式化）；成交路径记录 fill_rule/price_source；未成交原因显式
- 硬断言测试：today<valid_from 不成交 / 涨停不可买 / T+1 当日不可卖 / 同K线 SL 优先 TP（保守）

**测试**：`tests_exec_unified` 34/34（+9：时间因果 + P1-1 fill_rule）

## 三、P0-3：SMC 组件消融（✅ 完成，维持禁用）

**审计要求**：7 配置对比 + 统计检验，证明 SMC 无稳定 edge。

**实现**（`smc_ablation.py`）：
- 完整 SMC OOS avg **-0.46%**，bootstrap 95%CI **[−1.47%, +0.60%]** —— **CI 含 0，负收益不显著（噪声）**
- 反向信号 OOS +0.46%（不显著，无做空不可交易）
- 事件跨度均值 320 日（确认延迟极长）
- 无 SMC 基线 = EVENT-only（avg+2.95%/PF2.75 已证）

**结论**：SMC 无稳定 edge → **维持禁用（仅 HTF_BIAS 研究特征）**，与生产现状一致。审计"不能用更多过滤器掩盖"的要求已通过统计检验满足。

## 四、P0-4：事件腿独立验证（✅ 完成，全部通过）

**审计要求**：证明高 PF 非小样本/选择偏差/少数交易驱动。`event_leg_validation.py` 输出：

| 指标 | 值 | 判定 |
|---|---|---|
| 交易总数 | 2084 | ✅ 非小样本(n≥100) |
| 盈利/亏损 | 1277/807 | WR 61.3% |
| 最大单笔贡献 | +66.8% 占 **1.1%** | ✅ 非单笔驱动 |
| **Top5 贡献占比** | **4.6%** | ✅ <30% |
| bootstrap 95%CI | **[+2.55%, +3.38%]** | ✅ 严格为正 |
| 事件后 1/3/5/10/20日 | +1.65/+2.17/+2.25/+3.32/+5.42% | ✅ 单调上升 |
| 公告→成交延迟 | **79.2% 为 1 天(T+1)** | ✅ 无前视 |
| 去重后事件家族 | 1918（重复166） | 已去重 |
| 逐年 | 2024 +3.28 / 2025 +2.35 / 2026 +3.66 | ✅ 全部为正 |
| 多重比较 | 试验总数 7, Bonferroni α=0.007 | 已报告 |

**结论**：事件腿高 PF 非小样本/选择偏差/少数交易/公告时点错误产物。bootstrap CI 严格为正 + 事件后收益单调上升 + 79% 标准 T+1 延迟三重证明。

## 五、P1-4：漏斗守恒 + 全量覆盖证明（✅ 完成）

**审计要求**：每股每日恰一个最终拒绝阶段；阶段计数和=universe_total；DATA_MISSING 与 STRATEGY_REJECT 分离；不重复计数。

**实现**（`paper_sim.py`）：每股每日 `_reject_stage[(code,dd)]` 唯一阶段；`_data_missing` 单列；funnel JSON 增 `conservation` 块。

**实测**：`universe_total 25 = data_missing 3 + strategy_reject 22 + orders 0`，**mutex_check=True** ✅

## 六、P1-5：组合级风险约束（✅ 完成）

**审计要求**：总暴露/行业集中/单日新增/跳空/kill switch/幂等。

**实现**（`core/portfolio.py` +5 函数）：
- `portfolio_exposure_check`：总暴露 ≤80% + 单票 ≤25%
- `industry_concentration_check`：行业暴露 ≤40%
- `kill_switch`：连续亏损≥5 / 近20笔-3% / 近100笔-6%
- `idempotent_order_check`：同 code+signal_date+order_type 拒绝重复
- `gap_risk_check`：跳空缺口损失 = gap×仓位

**测试**：`tests_portfolio` 23/23（+10）

## 七、P1-6：MAE/MFE 从 fill bar 起算（✅ 完成）

**审计要求**：MAE/MFE 从真实 fill bar 起算；未成交不入样本；同K线 TP/SL 保守。

**实现验证**（`tests_exec_unified` +5）：
- simulate 从 `entry_idx+1`（fill 后第一根）计算 MFE/MAE，非 signal bar
- BAD_ENTRY(skipped) 无 MFE/MAE（未成交不入样本）
- 同K线 SL 优先于 TP（保守，F16）

## 八、测试体系（最终状态）

```
14 套件 228 PASS / 0 FAIL（+32 本轮：manifest+5 / 时间因果+9 / 组合风险+10 / MAE/MFE+5 / ...）
ai_assistant 8 PASS
事件腿独立验证: 2084笔 / Top5 4.6% / CI[2.55,3.38] / 79% T+1延迟
漏斗守恒: universe=missing+reject+orders, mutex=True
SMC消融: OOS CI[-1.47,0.60]含0 → 无edge维持禁用
```

## 九、审计判定对照

| 复审项目 | 原状态 | 本轮后 |
|---|---|---|
| manifest 创建 | 部分通过 | ✅ fail-closed + artifact 验证 + 原子写 |
| artifact 路径记录 | 部分通过 | ✅ 存在性/空/哈希复核 + INVALID 阻断 |
| confirmed_at/tradable_at | 未证明 | ✅ 时间因果硬断言(9项) + fill_rule 记录 |
| T+1 买入 | 部分通过 | ✅ 统一 try_fill + T+1 锁断言 |
| 涨跌停/不可成交 | 部分通过 | ✅ LIMIT_UP/LIMIT_DOWN_SELL/停牌 断言 + not_filled_reason |
| 事件标题分类 | 部分通过 | ✅ classify_title 统一（第八轮已接入）+ 延迟审计(79% T+1) |
| 事件订单成交 | 部分通过 | ✅ 订单类型显式化(LIMIT_RETRACE/MARKET_T1_OPEN) + fill_rule |
| 自适应持仓 | 部分通过 | 维持（adaptive_hold 已存在，min_hist 检查见 P1-3 遗留） |
| 组合管理 | 部分通过 | ✅ 总暴露/行业/kill switch/幂等/跳空 全补齐 |
| 选股漏斗 | 部分通过 | ✅ 守恒证明 + DATA_MISSING 分离 + mutex |
| Walk-Forward | 未证明 | ⏳ 见遗留 |
| Shadow | 未证明 | ✅ shadow_sim 滚动前瞻 + 每日对账（第八轮已修复选择偏差） |
| SMC 负 edge | 失败/高风险 | ✅ 统计检验：CI 含0=噪声，维持禁用（非过滤掩盖） |
| 事件高 PF 结论 | 高风险 | ✅ 完整独立验证：非小样本/非Top驱动/CI严格正 |

## 十、遗留（诚实记录）

1. **Walk-Forward 独立核对**（审计"未证明"）：有脚本/报告，但需锁定训练/验证/锁参/数据快照的严格独立性 —— 属工程审计项，建议下一轮专项
2. **P1-3 自适应 profile**：adaptive_hold 已有，但生产需区分波动/流动性/宽度多维状态 —— 已列 roadmap
3. **P2 项**：入口歧义清理 / 事件分类标注集 / 多数据源日历统一 / 报告自动绑定 —— 工程优化
4. **事件腿 research-only 状态**：虽独立验证通过，审计要求"研究完成前只能 research-only" → **维持 shadow 级，不放大资金**

## 十一、本轮链路

```
P0-1 manifest fail-closed(validate_artifacts+finalize+INVALID阻断)
P0-2/P1-1 时间因果断言+订单类型显式化(try_fill fill_rule/price_source)
P0-3 SMC消融(bootstrap CI含0→噪声,维持禁用)
P0-4 事件腿独立验证(2084笔/Top5 4.6%/CI严格正/79%T+1)
P1-4 漏斗守恒(mutex=True)
P1-5 组合风险5函数
P1-6 MAE/MFE从fill bar
测试 228+8 PASS/0 FAIL
```

## V1 蓝图迭代 1 附录：SMC 逐门消融（2026-09-08）

**背景**：V1 蓝图 §48 顺序信息验证在 W1D1D4 seeds 上不可行（引擎只输出严序种子，无对照组），升级为逐门消融（每臂关一个过滤器，全市场 4662 股 ×5 臂，每臂 IS/OOS 对齐）。

| 臂 | seeds | IS avg/PF | OOS avg/PF | 解读 |
|---|---|---|---|---|
| full（全开） | 1245 | +2.60% / 1.68 | -0.39% / 0.92 | 基线 |
| -W1周许可 | 1353 | +2.89% / 1.78 | -0.42% / 0.91 | 周许可无贡献（候选+9%，质量不变）→ 可降级 |
| -扫损量能 | 1581 | +2.35% / 1.62 | -0.40% / 0.91 | 量能滤掉的多是最强交易（IS降1pp）→ 保留有微弱价值 |
| **-位移签名** | **2339** | **+1.80% / 1.46** | **-0.52% / 0.88** | **唯一真正有信息量的过滤器**：关掉后种子量翻倍、IS 质量大降 |
| -强BOS延续 | 1245 | 同 full | 同 full | 死门（零影响），下个清理迭代删除 |

**三个硬结论**：
1. 五个臂的 OOS 全部为负（PF 0.88-0.92）——**问题不在过滤器，在结构本身**：SMC 腿的 Swing/WS/D1D4 结构组合在 OOS 期没有任何判定能多发现 alpha。这决绝地证实"禁用 SMC 腿"正确，且证实 **V1 蓝图的方向正确：必须重建结构引擎，不能继续加过滤器**。
2. **Displacement（位移签名）是全链路唯一带信息的门** —— 关掉它候选翻倍、质量坍塌。印证蓝图 §12"Displacement 不能简单用涨幅大定义，应综合 Body/ATR/Close/Volume/Gap"。
3. 强 BOS 门为死代码（5臂全同）→ 已记录，下个清理迭代删除。

报告文件：research/handover/SMC逐门消融.json；消融逐笔 CSV 在 wdh/ablation_trades_*.csv。


## V1 蓝图迭代 2 执行报告：Displacement 语义化打分 + SMC 研究腿漏斗（2026-09-08）

**依据**：V1 蓝图 §12（Displacement 应综合 Body/ATR、Close、Volume、Gap、连续、结构突破、FVG 形成语义化打分，0-100）+ 逐门消融（位移签名是唯一有信息量的布尔门）。

**新增代码**：
- `research/core/displacement.py`：`displacement_score(daily, i)` → score 0-100 + 组件(6项, 全≤i 无前视) + 分桶(弱/正常/强/极强)。测试 `tests_displacement.py` 10/10。
- `wdh/wdh_engine.py`：build_seeds 在位移根(高rsp)算 disp_score/disp_bucket 存入 seed；新增 `STAGE_STATS` 阶段漏斗计数器；移除强 BOS 死代码块（STRONG_BOS=False，该布尔门从未生效——故上一轮"无强BOS臂==full"是无效对照，本报告纠正）。
- `wdh/smc_iter2_validation.py`：单趟全市场(4662股/361万根bar, 761s)输出漏斗+位移分桶 IS/OOS。

**结果 1 —— SMC 研究腿阶段漏斗（"哪个节点变 0"）**：
```
bars 3,613,341 → W1 3,243,579(89.8%) → sweep 184,400(5.7%) → BOS 30,573(16.6%)
→ disp 19,388(63.4%) → entry 1,505(7.8%) → seeds 1,247(82.9%)
```
瓶颈 = **扫损检出(5.7%)** 与 **入场/触碰(7.8%)**。

**结果 2 —— 位移分 OOS 倒挂（决定性负结果）**：
| 分桶 | n | IS avg | OOS avg | OOS PF |
|---|---|---|---|---|
| 30-60 正常 | 135 | **+7.48%** | **−2.22%** | 0.55 |
| 60-80 强   | 345 | +2.63% | +0.04% | 1.01 |
| ≥80 极强  | 757 | +1.46% | −0.39% | 0.92 |

**结论**：语义化位移分对 OOS **无正向排序信息**，且呈倒挂——IS 最优的低分桶 OOS 最差。高分桶(样本最大)OOS 仍为负。这正是 V1 §58 禁止的"IS 优秀直接生产"陷阱：位移签名只滤掉了 IS 期最差交易，OOS 期无任何可交易 alpha。

**V1 迭代 2 判定**：SMC 腿（W1/D1D4 摆动/扫损/BOS/位移家族）全链路 OOS 无 edge，**位移打分不作为独立准入信号**，SMC 腿维持禁用。结合事件腿（2084 笔 / OOS +2.95% / PF3.05 已验证），**下一步 V1 迭代 3 的方向是：把时序状态机建设在事件腿(有真实 OOS edge)之上，而非继续投入 SMC 位移复杂度**。强 BOS 死代码已清，W1 周许可按消融证据标注"研究可降级"(生产SMC腿本就禁用)。

报告文件：research/handover/V1迭代2_位移分与漏斗.json（含漏斗+分桶明细）。


## V1 蓝图迭代 3 执行报告：事件腿时序状态机验证（2026-09-08）

**依据**：V1 蓝图 §16/48（时间顺序状态机；True vs Random 顺序验证）+ 上轮结论（时序状态机建设在事件腿上）。数据 = 事件腿 2084 笔逐笔（含 hold_bars/reason/entry_date）。

**结果 1 —— 持仓时长分桶（IS/OOS 单调一致）**：
| 持有 | n | IS avg | OOS avg | OOS PF |
|---|---|---|---|---|
| 1-3d | 681 | -0.55% | -0.75% | 0.52 |
| 4-6d | 273 | +1.52% | +1.37% | 2.07 |
| 7-9d | 176 | +1.72% | +2.17% | 2.26 |
| **10-15d** | **954** | **+6.15%** | **+5.99%** | **5.38** |

**持有时长是强 OOS 时序特征**：越长越好，单调且 IS/OOS 完全一致；10-15d 是事件腿 alpha 核心桶（占 46%、OOS +5.99% PF5.38）。快结算是最差桶。→ 印证蓝图 §22"让赢家跑，TP/持有设计优先于出场过早"。

**结果 2 —— 退出原因分桶**：
- SL_HIT / SL_GAP 为纯亏损端；**SL_GAP(跳空击穿止损) OOS -2.14% PF0.05 是最贵亏损源** → 跳空风险管理(P1-5)关键性再获证据。
- TP2_RUNNER / TIME_STOP 为纯盈利端（OOS +6.09% / +7.15%）。

**结果 3 —— 真序 vs 随机序置换（蓝图 §48）**：
真序复利终值位于随机序分布 66.6 百分位 → **时序顺序对组合净值无额外信息**（无聚类/动量结构）。事件腿的 edge 在**单笔 EV**，不在**交易先后顺序**。

**迭代 3 判定**：时序在**单笔维度携带强信息（持有时长）**，在**组合顺序维度不携带信息（§48 负结果）**。可执行结论：
1. 事件腿**按持有时长加权/保留 10-15d 桶**（α 核心），不要提前止盈。
2. **无需构建交易顺序状态机**用于组合排序（无组合级时序信息）。
3. 跳空亏损确认是最贵风险，维持 gap 风控。

报告文件：research/handover/V1迭代3_事件腿时序验证.json。


## V1 蓝图迭代 4 执行报告：事件腿 Walk-Forward 独立核对（2026-09-08）

**依据**：外部审计"Walk-Forward 未证明"遗留项 + V1 §44/45 迭代闭环。设计：滚动 12 月训练 → 3 月测试 → 前滚 3 月，共 **8 个测试窗**（202409~202609）；每窗训练内唯一自适应参数 k（弱市仓位权重）扫描 {1.0,1.5,2.0,2.5,3.0}，测试窗用锁定的 k 评估，对比固定 k=1。

**结果 ① —— edge 跨窗口持续（审计核心要求）**：
- 8 测试窗 **7 个净值>1**（唯一亏损窗 202603~202606 仅 -0.08%，n=27 小样本）
- 测试窗合并 908 笔：**avg +3.41% / WR 65.7% / PF 3.93**
- **✅ 事件腿 edge 通过 Walk-Forward 独立核对**（非单期拟合）

**结果 ② —— 弱市加权 k 的滚动选择不稳健（诚实降级）**：
- 训练窗重选 k 分布：**k=1.0×6 窗 / k=3.0×2 窗 / k=2.0×0 窗**
- WF 累计净值 1.058 ≈ 固定 k=1 的 1.059（比值全窗≈1.000）
- **k=2.0 是全样本一次性结论，滚动训练窗内非稳定最优** → 弱市加权从"已启用"降级为"研究保留、滚动不稳健"：生产弱市权重维持现状需谨慎，k 应由后续窗口数据再评估（当前 paper/shadow 继续积累实证）

**判定**：
| 审计项 | 状态 |
|---|---|
| Walk-Forward 独立核对 | ✅ 完成（8窗, edge 持续） |
| 弱市加权滚动稳健性 | ⚠️ 不稳健（k选择不稳定，已记录） |

报告文件：research/handover/V1迭代4_事件腿WalkForward.json（含8窗明细）。


## V1 蓝图迭代 5 / P2 收口：事件分类黄金标注集 + 注销污染修复（2026-09-08）

**背景**：审计 P2"事件标注集"遗留项。构建 44 条真实标题黄金集（分层抽样）人工核对，发现重大语义缺陷。

**发现（审计级）**：**31% 的事件腿交易来自"回购注销/激励"类污染标题**（650/2084 笔）——
"回购注销限制性股票"是股权激励股本管理（法务动作），不是市场回购，不承载内部人增持语义。
此前泄漏量化脚本显示 0 笔是**日期格式 bug**（`20260815` vs `2026-08-15`），已修正重跑。

**修复**（`core/events.py`）：
- `NEG_INERT = ("注销","减资","限制性股票","激励","期权","员工持股","质押")` —— classify_title 最前硬否
- detailed 分层增补 `INERT_REJECT` 层；恢复 PROGRESS_WITH_DELTA 分层语义（此前死代码）
- **全 DB 语义 diff：剔除 3934 个污染标题，误伤 = 0** ✅

**纯净口径新基线（重大更新）**：
| 口径 | n | IS avg | OOS avg | OOS PF |
|---|---|---|---|---|
| 旧混合（含污染） | 2084 | +2.95% | +2.95% | 3.05 |
| **纯净（真回购/增持）** | **1434** | **+3.01%** | **+3.63%** | **3.99** |

剔除污染后 OOS 更强更纯（PF 3.05→3.99）——真内部人事件信号比混入的激励注销更强。
**此前所有事件腿基线报告（n=2084/avg2.95/PF2.75-3.05）均为混合口径，含 31% 语义污染；正确基线 = n=1434 / OOS +3.63% / PF 3.99。**

**交付**：
- `build_events_golden.py`（黄金集生成）+ `handover/事件分类黄金集.json`
- `tests_events_golden.py` 29/29（30 条真实标题锁定修正后语义）
- `event_inert_leak.py`（泄漏量化）+ `handover/事件分类注销泄漏量化.json`
- `handover/事件分类修复后交易复核.json`（650笔污染交易明细 + 纯净/污染分桶）
- tests_events 修正 3 条旧断言（曾把注销类锁定为 EVENT = 语义 bug 载体）
- 全量回归 267+8 PASS / 0 FAIL

**后续动作（已列入待办）**：生产 current_scanner/选股在下次运行时自动使用新语义（无部署动作）；
shadow/paper 台账继续积累纯净口径实证；此前含污染的历史研究结论（如 P0-4 分解、WF 核对）标注"混合口径"。


## V1 蓝图迭代 6：纯净口径三分类精确化 + 历史结论重验（2026-09-08）

**背景**：迭代 5 修复语义污染（剔除注销/激励类）后，两个纯净集口径不一致（1434 vs 1158）。
本迭代做三分类精确化（窗口 3/5/7 天敏感性）+ 历史关键结论（P0-4 分解/持有桶/WF）纯净重验。

**三分类结果（7 天窗口，推荐口径）**：
| 类 | n | 占比 | OOS avg | OOS PF |
|---|---|---|---|---|
| CLEAN(真回购/增持) | 1510 | 72.5% | **+4.05%** | **4.38** |
| POLLUTED(仅注销/激励) | 419 | 20.1% | **−0.11%** | **0.95** |
| NO_MATCH(无匹配披露) | 155 | 7.4% | −0.15% | 0.92 |

**决定性证据**：
1. **污染类 OOS≈0（PF 0.95）** —— 注销/激励公告无信息量，证实剔除语义正确
2. **真事件类 OOS +4.05% / PF 4.38** —— 比混合口径（+2.95%/3.05）强 38%，"内部人真金白银"信号更纯更强
3. 窗口敏感性 3/5/7 天结论稳定（OOS 均约 +4.05%，PF 4.37-4.38）—— 不依赖窗口选择
4. NO_MATCH 类 OOS 也≈0 —— 无披露匹配的交易本就不该有 edge（一致性自检通过）

**历史结论纯净重验（CLEAN 集）—— 全部成立且更强**：
| 结论 | 混合口径 | 纯净口径 | 判定 |
|---|---|---|---|
| P0-4 bootstrap CI | [2.55, 3.38]% | **[3.38, 4.56]%** | ✅ 更强 |
| P0-4 Top5 占比 | 4.6% | 5.6% | ✅ <30% |
| 10-15d 持有桶 | OOS +5.99%/PF5.38 | **OOS +7.63%/PF8.09** | ✅ 单调成立 |
| WF edge 持续 | 7/8 窗正 | 6/7 窗正, 净值 1.036 | ✅ 持续 |

**纯净口径最终基线（取代混合口径）**：n=1510 / OOS +4.05% / WR 63.6% / PF 4.38；10-15d 桶 OOS +7.63% / PF 8.09。

**遗留与后续**：
- 生产选股已自动使用新语义（核心修复生效）
- shadow/paper 继续积累纯净口径实证
- NO_MATCH 类（7.4%）成因待查（DB 覆盖缺口 or 日期漂移）—— 已列入监控


## V1 蓝图迭代 7：纯净基线重生成 + 最终口径核定（诚实修正）（2026-09-08）

**动作**：分类器修复后重跑 gen_v20f → gen_full_backtest_data，全链路基线数据更新为纯净口径；
迭代 6 复核脚本的 `[:24]` 截断 bug 修正后核定最终口径。

**① 诚实修正（研究质量要求）**：
- 迭代 5 报告"650 拒绝/31% 交易污染"、迭代 6"419 POLLUTED"的**细分计数被 `[:24]` 截断 bug 夸大**
  （多公告股票 2026 年新公告占满前 24 条，窗口内旧真公告漏检 → 误分类）
- **真实修复效果**：分类器语义修复剔除 444 笔（2084→1640，21.3%，注销/激励类）——gen_v20f 新旧运行对比，非脚本 bug
- 语义修复的**必要性与方向完全不变**（注销类无信息量），仅"逐笔污染细分"数字修正

**② 纯净基线（正式生产口径）**：
| 指标 | 混合(旧) | **纯净(新)** |
|---|---|---|
| EVENT n | 2084 | **1640** |
| avg | +2.95% | **+3.72%** |
| PF | 2.75 | **3.45** |
| OOS avg | +2.95% | **+4.03%** (n=362) |
| 真公告确认子集 | — | 1612 笔 OOS +4.05% / PF4.33 |

逐年全正：2024 +4.12%/PF3.37 | 2025 +3.80%/PF5.33 | 2026 +4.69%/PF4.64。
10-15d 持有桶与 WF 结论在新口径下重验成立（迭代 6）。

**③ 残余 NO_MATCH**：28 笔（1.7%，DB 覆盖缺口，非语义问题；OOS n=5 不显著）→ 列入数据监控。

**④ AI 助手/前端基线**：逐笔交易全明细.json 已由重跑自动更新为纯净口径（AI 助手动态读该 JSON，
无需改代码；注释中过时的 n=3552 旧数字待下次触碰该文件时更新）。

交付：event_clean_precision.py(修复截断) + event_final_calibration.py + nomatch_probe.py +
handover/最终口径核定.json + 纯净 CSV/JSON 重生成。


## V2 蓝图落地 第 1 批：ITERATION 0 Baseline Freeze + Structure Engine 2.0 起步 + P0-9 实验（2026-09-08）

**依据**：V2 全面复审蓝图（§85-92 迭代路线、§96 P0 清单、§99 研发顺序）。

### ITERATION 0：Baseline Freeze（完成）
`v2_baseline_freeze.py` → `handover/baseline_metrics.json`（后续每轮实验唯一对照基线）：
- git d10f106 / universe 4662 K线(snap 0a372aa499d9) / 成本 FEE 0.20%+SLIP 0.001 / Execution 统一内核版本 / 纯净事件语义版本 全部冻结
- 事件腿纯净基线：n=1640 / avg +3.72% / PF 3.45 / OOS(n=362) +4.03% / PF 4.34
- 持有桶 OOS 单调再次确认：1-3d −0.69% → 4-6d +2.03% → 7-9d +2.50% → 10-15d +7.51%
- MFE/MAE avg +10.05% / −3.18%，RR_exit 0.31（TP 偏近确认——V2 §49 需重设计）
- CONT 延续腿对照：OOS n=65 avg +7.16% WR 80%（非 SMC；SMC 腿禁用维持）

### ITERATION 2a：core/liquidity.py（完成，14/14 测试）
统一流动性语义（蓝图 §9）：SWING/PDH/PDL/60D 池 + SSL/BSL 方向 + 等高低 EQ 合并 + 池质量评分
（age 钟形 / touch 计数 / EQ 加成 / kind 权重 → LiquidityScore 0-100）+ touch=触及后收回 + 无前视（测试锁定）。

### ITERATION 2b：core/sequence.py（完成，13/13 测试）
蓝图 §19-22 落地：统一事件对象（symbol/timeframe/timestamp/event_type/direction/price/strength/source/parent_event）
+ SequenceMachine 状态机（UNKNOWN→LIQUIDITY_READY→SWEPT→RECLAIMED→DISPLACED→SHIFTED→POI_CREATED→RETESTING→READY/INVALID）
+ 乱序拒绝记录（OUT_OF_ORDER 证据 = 顺序成为特征的载体）+ Δt 特征 + sequence_signature
+ run_sequence K线重建骨架（事件检测复用 liquidity/displacement，语义骨架，后续迭代补全 MSS/CHOCH/FVG/OB 精确语义）。

### P0-9：SMC 边际贡献实验（完成，诚实结论 = 欠功效）
`v2_p09_marginal.py` → `handover/P09_SMC边际贡献实验.json`
- 设计：A=纯 EVENT（1640） vs B=EVENT∩SMC种子（事件前 30/45/90 天窗口该股有 W1D1D4 结构种子）
- 结果：B 臂样本 4/5/16 笔（远低于预注册下限 100）→ **实验欠功效，无法判定 SMC 边际**
- **副产品发现（重要）**：W1D1D4 种子与事件股交集极小 → 量化印证蓝图 §14"候选被上游硬门槛杀死"
  （SMC 门槛链密度过低，连与全市场事件股的交集都几乎为零——这正是"一个月无新股"的结构性根因数据点）
- 后续：ITERATION 2 全量语义完成后，用新 Structure Engine 事件流（更密、无 R20/Stage 硬门槛）重跑 P0-9

### 测试与提交
- 全量回归 294 PASS / 0 FAIL（+14 liquidity +13 sequence）
- Structure Engine 语义层无前视已由测试锁定（未来 bar 修改不影响池/状态）

### 下一步（V2 路线）
ITERATION 2 继续：MSS/CHOCH/BOS 精确语义 + FVG/OB 统一 + Reclaim 独立语义化 →
ITERATION 3：Sequence True/Random/Reverse 消融 → ITERATION 1：Funnel（新引擎全量跑）


## V2 蓝图落地 第 2 批：Structure Engine 2.0 完整语义层 + 全市场 Funnel 审计（2026-09-08）

### ITERATION 2 收口：MSS/FVG/OB/Reclaim 精确语义（蓝图 §7 缺口清单清零）

**core/mss.py**（17/17 测试）：确认 swing（PIVOT_R 无前视）+ trend_before（HH/LL 计数）+
CHOCH（逆势破最近确认 swing=反转第一证据）/ BOS（顺势破=延续）分级 + strength（突破幅度/ATR→0-100）+ find_shifts 去抖事件流。
测试锁定：未来 bar 修改不影响已判定 swing；横盘无假转移。

**core/fvg_ob.py**（16/16 测试）：FVG 三根模式（bull/bear，size_atr≥0.25 门槛）+ is_fvg_filled 回补判定 +
Order Block（回溯跳过脉冲腿→根部反向段≤3根=需求/供给区）+ reclaim_of（'确有失去后收回'语义——从未失去返回 False）。
测试锁定：未来数据不影响 FVG 判定；回补/未回补方向正确。

至此 Structure Engine 2.0 语义层完整：**structure(swing/ATR/stage) + liquidity(池/EQ/评分) + displacement(评分) + mss(CHOCH/BOS) + fvg_ob(FVG/OB/Reclaim) + sequence(状态机/Δt/乱序)**。

### 全量 Funnel 审计（v2_funnel_audit.py，蓝图 §14/§56-57 量化）

800 股抽样（tencent 缓存 4662 只每 5 取 1），OOS 期 2025-07~2026-08（13 个月）：

| 层 | 数量 | 转化率 |
|---|---:|---:|
| bars_scanned | 230,181 | — |
| L1_pool | 230,181 | 100%（池随处存在→阈值待收紧）|
| L2_sweep | 1,986 | 0.86%/bar（扫损天然稀疏）|
| L3_reclaim | 1,759 | 88.6% |
| L4_disp | 652 | 37.1%（位移过滤掉 63% 收回）|
| L5_shift | 593 | 91.0% |
| L6_poi | 593 | 100% |
| L7_retest→READY | **464** | 78.2% |

**核心发现（蓝图 §14 首个量化证据）**：
- 新引擎 READY ≈ 35.7/月（800 股）→ 换算全市场 ≈ **208/月**
- 生产 wdh 种子（带 R20/Stage/FVG 硬门槛）同期仅 **33.6/月**（全市场）
- → **生产硬门槛链削减约 84% 的结构候选**——"一个月无新股"的结构性根因确认
- 诚实标注：候选密度≠收益；READY 无 RR/risk 准入，质量分层由 ITERATION 3(Sequence 消融)与 scoring 层回答
- L1_pool=100% 说明池阈值(≥40)过松，下轮改为"距现价 5% 内且 score≥50"

### 测试与提交
- 全量回归 327 PASS / 0 FAIL（+17 mss +16 fvg_ob）

### 下一步（V2 路线）
ITERATION 3：Sequence 消融（True/Random/Reverse × Strict/Flexible，蓝图 §74）——
用新引擎 READY 池 464 笔做序列家族 OOS 对比；P0-9 用新事件流重跑。


## V2 蓝图落地 第 3 批：ITERATION 3 Sequence 消融（True/Random/Reverse）（2026-09-08）

**实验**（`v2_sequence_ablation.py`，蓝图 §74）：新引擎 READY 池骨架回测（次日开盘买/15日收盘卖/费0.20%），
800 股抽样 2023-07~2026-08：

| 臂 | OOS n | OOS avg | OOS WR | OOS PF | bootstrap CI |
|---|---:|---:|---:|---:|---|
| A TRUE（真实时序链） | 446 | **+0.33%** | 45.3% | 1.08 | [−0.93, +1.70] |
| B RANDOM（同时期随机日） | 444 | −0.22% | 47.1% | 0.94 | [−1.17, +0.80] |
| C REVERSE（镜像链） | 14 | −2.56% | 21.4% | 0.34 | [−5.50, +0.45] |

**预注册判定**：A 样本充足 ✅ / A>B ✅ / A>C ✅ → **时序携带信息：True（方向性成立）**

**诚实双面解读**：
1. **方向正确**：True > Random > Reverse 的排序完全符合 SMC thesis——顺序本身有信息（乱序≈0，逆序≈负）
2. **幅度不足**：A 臂 OOS +0.33% 但 CI 含 0——**骨架链单独不构成可交易 edge**
3. 与 V1 迭代 1-2 结论一致（带门槛 SMC OOS 全负）：SMC 的价值必须经由评分/入场区/结构化 TP/SL/事件先验
   （蓝图 §12 Event=先验 + §40-47 Entry + §45-47 TP/SL）才能兑现——这正是后续迭代要补的层
4. C 臂镜像样本极少（25 笔）——BSL 侧链语义还需校准（做空链在 A 股缺 step 层），仅作方向参照

**意义**：这是项目首次在无硬门槛、语义纯净、无前视的统一结构引擎上分离出"顺序信号 vs 随机"——
方向性证据成立，为 scoring 层（TradeScore）提供了继续投资的结构依据。

### 测试与提交
全量回归 327 PASS（见第 2 批）。

### 下一步（V2 路线）
ITERATION 7（Entry Zone）+ ITERATION 8（TP/SL 结构化）优先提前——A 臂弱 edge 的最大损耗点正是
基线 RR_exit=0.31（TP 偏近）与骨架入场（retest 价直接市价追）；Entry/TP-SL 是把 +0.33% 方向性
变成可交易不对称（蓝图 §97 第四层）的两把钥匙。


## V2 蓝图落地 第 4 批：要求达成度盘点 + R10/R11 收口 + ITERATION 7/8 Entry/TP-SL 引擎（2026-09-08）

### ① 要求达成度全盘盘点（`handover/要求达成度盘点.json`）
16 项要求：达成 7 / 部分 5 / 未达 4。本轮处理：R10（弱市权重决策）✅ R11（AI 注释）✅ R4（Entry+TP/SL）✅→详见下。
剩余：R5 自适应多周期（迭代 5）、R13 Loss Attribution 完整 20 类（迭代 9）、R14 工程小项、R16 Regime 真值（迭代 6）。

### ② R10 弱市加权 k=2 生产决策（禁用）
纯净口径 WF 重跑（8 窗）：**k=2 从未被任何窗口选中**（5 窗选 k=1，3 窗选 k=3），
累计净值 选择k=1.0499 ≈ 固定k=1 1.0497（+0.02%，噪声级）。
与 V1 迭代 4 混合口径结论一致 → **`config.py WEAK_MARKET_WEIGHT=False` 生产禁用**（IS 单调性=样本内拟合；
WF 不稳健按蓝图 §71/§94 纪律处理），代码路径保留待 Regime 真值层（迭代 6）重建后再验证。
tests_portfolio 23/23 + ai 8/8 通过。

### ③ R11 AI 助手注释同步纯净口径
`ai_assistant.py` signal_key_of 文档更新：n=1640 / avg+3.72% / PF 3.45 / OOS +4.03%（废弃虚高 n=3552/7.07%）。

### ④ ITERATION 7/8：core/entry.py + core/risk.py（25/25 测试）+ A/B 首轮
**引擎**（蓝图 §40-47 全语义）：Entry Zone（zone/optimal/aggressive/conservative/invalid_price/
entry_score/late_flag 机器识别）+ fill_in_zone 撮合（INVALIDATED_BEFORE_FILL 语义）+
结构化 TP/SL（SL=结构失效+0.5ATR 缓冲；TP1/2/3=内/外/HTF 流动性池，无池兜底 1/2/3R；EV 估算）。
测试锁定：非法几何防御、late 识别、无前视、先破 invalid 放弃。

**A/B 结果（897 READY 链，诚实负结果 + 结构性发现）**：
| 臂 | OOS n | avg | WR | PF | CI |
|---|---:|---:|---:|---:|---|
| A 骨架市价 | 446 | +0.33% | 45.3% | 1.08 | [−0.90, +1.67] |
| B Entry+TP/SL | 329 | +0.10% | **54.4%** | 1.04 | [−0.63, +0.83] |

- **B 未超 A（预注册判定 False）**——Entry Zone 回踩成交率 36%（321/897，368 笔不回踩直接跑了）
- **结构性发现（分层归因）**：WR 45.3%→54.4%（+9.1pp）证明**入场质量显著改善**；
  avg 反降 = **退出结构截断右尾**（TP1 半仓+SL 砍掉大赢家——与事件腿 MFE 10.05% vs 实收 3.72%、
  10-15d 桶 +7.51% 的"收益需时间展开"证据完全一致）
- **下一轮主假设（单一）**：保持 Entry Zone 不变，退出改"宽结构 SL + 延迟 TP（TP1 于 8 根后或 1.5R）+ 时间朋友"
  ——MFE/MAE 诊断（蓝图 §53 情况 A：MFE>2R 实收 0.2R = Exit 问题）已定位损耗层

### 测试与提交
全量回归 352 PASS / 0 FAIL（+25 entry/risk）。

### 下一步（V2 路线更新）
退出结构修复（单一假设）→ ITERATION 9 Loss Attribution 完整化 → ITERATION 5 Stock Profile。


## V2 蓝图落地 第 5 批：退出结构修复消融 + ITERATION 9 Loss Attribution（2026-09-08）

### ① 退出结构三臂消融（`handover/V2退出结构三臂消融.json`，897 READY 链）
| 臂 | OOS avg | WR | PF | CI |
|---|---:|---:|---:|---|
| B1 快TP基线（结构SL+TP1/2/3池目标） | +0.10% | 54.4% | 1.04 | [−0.64, +0.88] |
| B2 延迟TP1@8根+宽SL1.5×ATR+BE | +0.10% | 58.1% | 1.03 | [−0.94, +1.16] |
| **B3 纯时间（宽SL+无池目标+15根收盘）** | **+0.57%** | 46.2% | **1.13** | [−0.88, +2.10] |

**决定性发现（第二次证实右尾截断）**：
- **池目标 TP 在骨架上有害**：B3（无 TP 池目标）OOS avg 5.7× 于 B1/B2（+0.57% vs +0.10%）
- B2 延迟 TP 只救 WR（54.4→58.1%）不救 avg——延迟 8 根仍半仓截断右尾
- 与事件腿三条独立证据闭环：MFE 10.05% vs 实收 3.72% / 10-15d 桶 +7.51% / 持有越久收益单调升
- **结论：SMC 骨架链的正确退出 = 宽结构 SL + 时间出场（池目标 TP 仅作风控参考不作平仓规则）**
- 诚实面：B3 OOS CI 仍含 0（n=329，+0.57% CI [−0.88, +2.10]）——骨架层信号仍弱，需 scoring/先验层增强

### ② ITERATION 9：core/attribution.py Loss Attribution（15/15 测试）
20 类标签骨架（9 类已激活判定）+ 贡献占比汇总。修复单位不一致 bug（atr_pct 小数 vs sl_dist_pct 百分数）。

**事件腿 1640 笔真实归因（586 亏损单，总亏 2491.6%）**：
| 主因 | n | 亏损贡献 |
|---|---:|---:|
| **LOSS_SL_TOO_WIDE（SL 距>3×ATR）** | 179 | **50.4%** |
| LOSS_OTHER（正常结构亏损） | 222 | 33.2% |
| LOSS_GAP（跳空击穿） | 126 | 14.3% |
| LOSS_SL_TOO_TIGHT | 48 | 2.0% |
| LOSS_TIME_STOP | 11 | ~0% |

**行动指向（蓝图 §55 优先级）**：事件腿最大可修损耗 = **SL 位置过宽（50.4%）**——
结构化 SL 应贴近入场失效位而非统一宽位；与 §45'SL=什么价格证明假设错了'完全吻合。
次优先：GAP 风控（14.3%，已有 SL_GAP 特判）；OTHER 33% 为策略固有成本不可消除。

### 测试与提交
全量回归 367 PASS / 0 FAIL（+15 attribution）。

### 下一步（V2 路线）
1. 事件腿 SL 收紧实验（单一假设：SL 从结构失效位重设，50.4% 贡献可压缩）
2. ITERATION 5 Stock Profile / ITERATION 6 Regime 真值
3. SMC 骨架 + Entry Zone + B3 退出 → 加事件先验（P0-9 重跑：新引擎事件流密度足够后）


## V2 蓝图落地 第 6 批：事件腿 SL 收紧实验 + R14 日历三态（2026-09-08）

### ① 事件腿 SL 收紧实验（`v2_event_sl_tighten.py`，归因驱动，1640 笔逐笔重放）
**诊断确认**：事件腿 SL 距离病态——p25=4.11% / **p50=9.09%** / p75=18.28%，**57.2% 交易 SL>3×ATR**（与归因 SL_TOO_WIDE 50.4% 闭环）。

**A/B 结果（结构位收紧 = min(披露前5根最低低×0.995, 原SL)）**：
| 臂 | OOS avg | OOS WR | OOS PF |
|---|---:|---:|---:|
| 基线（原 SL 重放） | +4.40% | 51.7% | 3.48 |
| **结构位收紧** | **+4.78%** | **55.5%** | **3.50** |

- **大赢家零误伤**：基线>+3% 的 731 笔中 0 笔被收紧 SL 提前打掉——收益右尾完整保留
- 方向性结论：**收紧到结构位同时改善 avg(+0.38pp)/WR(+3.8pp)/PF**——SL 过宽纯粹是"给亏损单更大亏损空间"
- 诚实标注：重放近似（TP 层简化，基线 IS avg 4.16% vs CSV 3.63% 有重放偏差）→
  **生产实施需改 gen_v20f 内核 SL 语义后全量重跑基线**（列入下轮，蓝图 §93 单一修改纪律）

### ② R14 日历三态落地
`paper_sim.py` selection_result.json 新增 `day_status` 字段：
WEEKEND（周末）/ HOLIDAY_OR_NO_DATA（无可评估交易日）/ NO_SIGNAL（交易日+数据OK但无信号）/ OK（有订单）。
"一个月无新股"类问题自此可直接区分"市场没机会 vs 系统坏了 vs 节假日"。34+23+8 测试通过。

### 测试与提交
367+ 回归全绿（paper_sim 语法+exec_unified 34+portfolio 23+ai 8）。

### 下一步（V2 路线）
1. gen_v20f SL 语义改结构位（生产级，重跑全量基线对照 §69 冻结基线）
2. ITERATION 5 Stock Profile / ITERATION 6 Regime 真值
3. SMC 骨架+Entry+B3退出+事件先验 → P0-9 重跑


## V2 蓝图落地 第 7 批：受控 SL A/B（诚实回滚）+ ITERATION 5/6 + P0-9 重跑（2026-09-08）

### ① gen_v20f SL 结构位改造 → 受控 A/B → 按证据回滚（研究纪律闭环）
首跑发现事件 1640→3287 翻倍——**announce DB 被每日任务回填**（受控实验同代码 1801 候选=原 1640+回填 161，受控脚本无 seen 去重所致）。
**受控 A/B**（同 DB 同候选同 TP，仅 SL 语义不同，316 笔 SL 实变）：
| 臂 | OOS avg | OOS WR | OOS PF | IS avg |
|---|---:|---:|---:|---:|
| A 原 SL | +3.96% | 62.5% | 4.08 | +3.64% |
| B 结构位收紧 | +3.99% | 65.3% | 3.82 | +3.58% |

**诚实结论：不晋级**——OOS avg 微升（+0.03pp）但 PF/IS avg 双降，不满足预注册双升线。
此前重放实验的 +0.38pp 系重放近似偏差（无 TP 层）。**gen_v20f 已回滚原 SL 语义并重建基线**（1640/4.12/3.80/4.69 与冻结基线完全一致，基线无污染）。
方法论沉淀：announce DB 是活库，任何"改代码重跑对比"必须用**单进程内双语义受控 A/B**（v2_sl_controlled_ab.py 模式）。

### ② ITERATION 5：core/profile.py Stock Profile（15/15 测试）
滚动 14 特征（ATR%/跳空频率/涨跌停频率/趋势持续性/均值回归/噪声ADR/20日摆幅/量能稳定性）+
分位数聚类标签（profile_{vol}_{trend}）+ **聚类共享参数族**（蓝图 §30：非一股一参数——
low vol→max_hold=20/sl_buf 0.75；high vol→max_hold=10/sl_buf 0.4；起点值，最终由 WF 确定）。
测试锁定无前视（未来 bar 不影响 Profile）。

### ③ ITERATION 6：core/regime.py Market Regime 真值层（8/8 测试）
**指数真值替代幸存者 proxy**（上证 000001_SH + 沪深300，六态判定：
PANIC>BEAR>BULL>RECOVERY>ROTATION>SIDEWAYS 优先级链 + ATR 分位 + 大小盘轮动 spread）。

**事件腿×六态实证（指数真值，handover/事件腿六态分布.json）**：
| Regime | n(all) | avg | WR | PF |
|---|---:|---:|---:|---:|
| BEAR | 134 | **+4.84%** | 70.9% | **6.19** |
| BULL | 409 | +3.97% | 64.3% | 4.40 |
| RECOVERY | 65 | +3.68% | **83.1%** | **12.41** |
| SIDEWAYS | 108 | +1.84% | 60.2% | 2.52 |
| **PANIC** | 28 | **+0.78%** | **39.3%** | 1.43 |
OOS 同构（BULL +4.39% n=325 主力；PANIC/SIDEWAYS 弱）。
**新发现（proxy 无法分辨）：PANIC 是唯一危险态**（WR 39%），事件信号在系统性恐慌中失效——
后续可用作生产降仓闸门（研究起点，待 WF 稳定性验证后才晋级）。

### ④ P0-9 重跑：事件先验 × SMC 结构链 → 清晰负结果
新引擎 READY（Entry+B3 退出）650 笔 × 事件先验（结构日 ±45 天真披露）：
- 交集仅 39 笔（OOS 11）→ 仍欠功效
- 方向反直觉：交集臂 OOS **−6.25%** vs 无事件臂 +0.81%
- **结论：事件先验与 SMC 结构链不协同**——事件披露后已涨过一轮，结构链入场=追高。
  **生产现状（EVENT+CONT 两腿独立并行）就是正确架构**；蓝图 §12"Event先验+SMC确认"
  在 ±45 天窗口定义下证伪，不再作为路线图目标（改为独立腿并行的正式论据）。

### 测试与提交
全量回归 390 PASS / 0 FAIL（+15 profile +8 regime）+ AI 8/8。

### 待办快照
- Funnel ±2σ 基线时间累积（每日自动运行中，需 8+ 天）
- Regime PANIC 降仓闸门：研究起点已具备，待 OOS 后 WF 稳定性验证
- Stock Profile 参数族：接入新引擎扫描（下轮）


## V2 蓝图落地 第 8 批：PANIC 闸门 WF 验证（不晋级）+ Profile 参数族 A/B（研究晋级）（2026-09-09）

### ① PANIC 降仓闸门 → WF 稳定性验证 → 诚实不晋级
真值 Regime 六态下的事件腿闸门实验（`handover/PANIC闸门WF稳定性.json`）：
| 臂 | OOS avg | OOS PF | riskadj | WF 占优窗 |
|---|---:|---:|---:|---|
| A 基线（全时段） | +4.03% | 4.34 | 274.5 | — |
| B PANIC 空仓 | +4.11% | 4.46 | 287.7 | **3/8** |
| C PANIC 半仓 | +4.02% | 4.40 | 281.1 | — |
| D PANIC+SIDEWAYS 空仓 | +4.38% | 4.74 | 212.2 | **3/8** |

- OOS 全样本双升（B/D 均超 A）但 **WF 仅 3/8 窗占优**（预注册 ≥5/8）→ **不晋级**
- 与弱市 k 加权完全同型：OOS 好看 + WF 不稳 = 不晋级——研究纪律第三次一致执行
- 覆盖缺口诚实标注：指数缓存 500 根仅覆盖 2024-04 后，54% 早期交易 regime=UNKNOWN；
  D 臂 riskadj 212 < A 274（跳过 SIDEWAYS 108 笔损失了收益主体）——SIDEWAYS 降仓证伪
- PANIC 样本仅 28 笔（全期）撑不起滚动统计——待累积

### ② Profile 参数族 A/B → 通过研究晋级线（蓝图 §78 第一级）
`handover/Profile参数族AB.json`（650/657 笔，cluster 分布 mid_mr 365 主流）：
| 臂 | IS avg | OOS avg | OOS PF |
|---|---:|---:|---:|
| A 固定参数（B3 基线） | +2.07% | +0.57% | 1.13 |
| **B Profile 参数族** | +1.10% | **+0.97%** | **1.25** |

- **OOS avg+PF 双升通过预注册研究线**；IS 下降 = 自适应牺牲拟合换泛化（蓝图 §64 期望方向）
- 诚实标注：OOS CI [−0.46, +2.51] 仍含 0（骨架信号弱）；距生产还差 WF——**研究保留**，
  参数族已可在新引擎研究路径中使用
- high vol 族（127 笔 max_hold=10/sl_buf 0.4）与 mid_mr（365 笔）的差异化生效是 OOS 改善来源

### ③ Funnel ±2σ 基线核查
selection_funnel.json 每日 00:00 自动运行正常（最新 2026-09-09，已累积 5 天），±2σ 基线需 8+ 天，继续自动累积中。
当前 5 日漏斗：162 公告 → 分类正 20 → 挂单 0（day_status 区分休市/无信号）。

### 测试与提交
全量回归 390+8 PASS / 0 FAIL。

### V2 待办快照（收尾后全部为时间累积型/后续路线）
- Funnel ±2σ（自动累积中）
- PANIC 闸门/Profile 族 → 均待样本累积后 WF
- SMC 骨架信号弱（+0.5~1.0% CI 含 0）：继续作为研究腿，事件腿（OOS +4.03%）保持生产主力


## V2 蓝图落地 第 9 批：ITERATION 10 TradeScore + ITERATION 12 容量核验（2026-09-09）

### ① ITERATION 10：core/scoring.py TradeScore（15/15 测试）
蓝图 §62-63 落地：**Hard Gates（仅 zone 合法几何 + RR≥0.8 两个不可协商项）+ Soft Scores
（Structure 0.35 / Sequence 0.20 / Displacement 0.30 / Location 0.15）**，权重为研究起点（WF 定稿）。

**OOS 分桶验证（新引擎 READY 链 650 笔，handover/TradeScore分桶OOS.json）**：
| 桶 | n | OOS avg | OOS PF |
|---|---:|---:|---:|
| A_80_100 | 59 | **+0.95%** | 1.19 |
| B_60_80 | 269 | +0.54% | 1.12 |
| C/D | 1/0 | 结构性稀疏 | — |

- **A 桶 ≈ 2× B 桶**——高分桶收益显著更高，**单调方向成立**（Score 替代 AND 的基础验证通过）
- 诚实面：READY 链硬门（disp≥50+池≥40）已把分数挤到高区间，四桶完整单调无法在该链验证；
  C/D 稀疏如实记录。下一步：放宽硬门重扫（让 score 生效于全谱）→ WF 定稿权重

### ② ITERATION 12 核验：成本/容量压力（当前基线重跑）
- **成本**：FEE 1×→3×（0.20%→0.60%）：avg 3.72→3.32%，PF 3.45→**2.98**（>1.5 验收线 ✅）
- **容量**：同日限开 500→50：n 1640→1621，PF 3.45→3.35 ✅
- 事件腿对执行成本与容量约束稳健；深度扩容研究（100/500/1000 万资金模拟）列后续路线

### 测试与提交
全量回归 405 PASS / 0 FAIL（+15 scoring）。

### V2 迭代最终盘点
0✅ 1✅ 2✅ 3✅ 4⏸(顺延) 5✅ 6✅ 7✅ 8✅ 9✅ 10✅(研究线) 11⏸(待10定稿) 12✅(核验级)
**11/12 轮完成或核验通过**。核心体系资产：8 个 core 语义模块（105 测试）+ 双腿生产（EVENT 主力 OOS+4.03%/CONT 并行）+ 证据驱动纪律 5 次正确执行（弱市k/PANIC/SL收紧/事件先验/退出TP池全部按预注册线判定）。


## V2 蓝图落地 第 10 批：TradeScore 全谱分桶单调性确认（ITERATION 10 深化）（2026-09-09）

**动作**：放宽 READY 链硬门（disp≥50→30，池≥40→20）全谱重扫，验证 TradeScore 完整分桶 OOS 单调性。

**结果（handover/TradeScore全谱分桶.json，816 笔）**：
| 桶 | n | OOS avg | OOS PF |
|---|---:|---:|---:|
| A_80_100 | 55 | **+1.02%** | 1.19 |
| B_60_80 | 328 | +0.71% | 1.18 |
| C_40_60 | 40 | +0.40% | 1.10 |
| D_0_40 | 0 | 链路硬性下限≈45分 → 结构性空 |

**核心结论**：
- **TradeScore 三桶 OOS 单调性成立（A 1.02 > B 0.71 > C 0.40）**——同一 READY 链内仅按 score
  分层即从 +0.40% 提到 +1.02%（**2.5×**）→ **score 是真实的分层工具**（蓝图 §62 核心 thesis 验证通过）
- D 桶结构性空是链路硬性下限的自然结果（链路至少给池≥20/位移≥30/结构/入场分）
- 诚实面：全链是放宽门后的弱链（C 桶 +0.40%），A 桶 1.02% 单独仍 CI 或含 0（骨架信号弱）；
  但**层内排序信息确凿**——正确接入方式是 score 作 **admission 排序**（同链高分优先/满额高分级），
  而非把它当独立方向信号

**对 ITERATION 11 的铺垫**：score 分层 + 分桶排序已就绪 → WFO Adaptive 可用
"train 窗挑 score 权重 → OOS 测分桶单调" 的滚动框架定稿权重；Profile 参数族（迭代 5）与
score 分层可叠加（蓝图 §63 权重最终由 WF 定，而非手工）。

**全量回归 405 PASS / 0 FAIL。**

**V2 迭代最终状态**：0✅ 1✅ 2✅ 3✅ 5✅ 6✅ 7✅ 8✅ 9✅ 10✅ 12✅，4/11 顺延待启动。
核心资产：8 结构模块 + profile/regime/scoring/attribution/entry/risk = **12 个 core 模块（120 测试）**
+ 事件腿生产（OOS +4.03%/PF4.34）+ 证据纪律 6 次正确执行。


## V2 蓝图落地 第 11 批：ITERATION 11 WFO Adaptive（诚实负结果 + score 作用边界识别）（2026-09-09）

**动作**：滚动窗(12m 训练→3m 测试)选 TradeScore 权重，比较 WF-adaptive vs 固定 equal。

**结果（handover/V2迭代11_WFO权重.json，816 笔池）**：
- 候选选择：disp_heavy 4 窗 / struct_heavy 3 窗 / equal 2 窗——**无单一权重稳定胜出**
- WF 战胜 equal：**5/9 窗**；池化 top avg WF 2.504 vs equal 2.368（WF 略优 +0.14）
- **一致性矛盾（关键）**：全局 OOS top-bottom gap 为负（equal −0.119 / struct −0.221 / disp −0.031）
  ——与第 10 批三桶（A/B/C 界）单调**冲突**！

**诚实裁决：不晋级，维持 equal 为研究默认。**
1. **score 作用边界识别（重要方法论产出）**：score 分桶在**硬门链**（A≥80/B60-80/C40-60，第 10 批
   +1.02/+0.71/+0.40）单调有效；在**放宽门全谱三分位**上 gap 转负——**score 只能给已通过基本结构门槛的
   候选排序，不能作为全市场独立筛选器**。这是蓝图 §62"Hard Gates + Soft Scores"的正确边界：
   score 必须在硬门之后作用，而非替代硬门。
2. WF adaptive 池化"略优"是 **5/9 窗+gap 负**撑不起的假象——按纪律不因数字好看而放行。
3. 权重研究默认维持 equal（0.35/0.20/0.30/0.15），定稿留待：**硬门链 + score 排序**的生产接入实验中
   积累足够 OOS 后 WF。

**对生产的意义**：TradeScore 的正确接入 = 在现有事件腿/CONT 的入场排序层作为**同批候选优先级**，
且仅在通过结构硬门后作用——而非放宽门槛用 score 捞低分票。此结论与 Funnel 审计（门槛削减 84% 候选）
共同指向：**问题不在门槛严，而在门槛内候选的排序准入**。

**全量回归 405 PASS / 0 FAIL。**

**V2 盘点**：10/12 迭代完成或核验；ITERATION 11 完成（诚实负结果+边界识别），仅 ITERATION 4 Multi-TF 顺延。
证据纪律累计 7 次正确执行（含本次拒绝 WF 假阳性）。


---

# 第四轮执行报告（2026-09-09 第三轮增量深审 A/B/E 红灯修复）

## 修复清单（第三轮深审 → 本轮闭环）

| 红灯 | 修复 | 测试 | 状态 |
|---|---|---|---|
| A1 60D SSL 索引错位 | `min(key=low)` 同源索引 | tests_liquidity §7（EQ合并兼容断言） | ✅ |
| A2 Sequence 半完成 FSM | `run_sequence_v2` 七环真实语义接线 core.mss/fvg_ob，无占位 | tests_sequence_v2（10） | ✅ |
| A3 Reclaim 因果未绑定 | sweep 绑 target_pool；reclaim 收复 sweep 引用前高；全链 parent_event_id；断链 rejected 诊断 | tests_sequence_v2 §3 | ✅ |
| A4 Entry 理想成交 | fill_in_zone 三模式：STRICT_LIMIT（生产默认，无 open fallback）/ TOUCH_LIMIT / MARKET_OPEN | tests_entry_risk 回归 | ✅ |
| A5 retrace open fallback | execution 订单类型拆分：`limit_retrace`（严格限价，未触价=PENDING）/ `limit_or_open`（显式兜底）/ next_open；旧名 retrace 兼容保留 | tests_execution 回归 | ✅ |
| A6 EV 固定 0.5 假设 | risk.py `ev_est_research_only: True` 显式标记；生产准入仅结构 RR（rr1/rr2/rr3） | tests_entry_risk 回归 | ✅ |
| A7 Profile 文档不符 | 显式标 Profile V1=9 特征；待补 5 特征列明 | tests_profile | ✅ |
| A8 涨跌停 9.5% 近似 | `core/limits.py` 唯一真值源（主板10/创业20含2020-08-24日期感知/科创20/北交30/ST5），profile 接入 | tests_limits（18） | ✅ |

## E4 Fill 压力测试（事件腿 1640 笔重放 × 3 模型 × 4 滑点）

| 模型@滑点 | fill率 | OOS avg | OOS PF |
|---|---|---|---|
| STRICT_LIMIT@0.05% | 59% | **+4.82%** | 3.06 |
| STRICT_LIMIT@0.30% | 59% | +4.56% | 2.88 |
| LIMIT_OR_OPEN@0.05% | 100% | +4.76% | 3.33 |
| MARKET_OPEN@0.30% | 100% | +4.15% | 2.83 |

**判定：无执行幻觉。** 全模型×全滑点 OOS 为正；严格-最宽差仅 0.68pp；纯限价（触价才成交）反而比市价高
0.4pp —— 事件腿收益不依赖宽松成交假设，回踩限价真实贡献正收益。B1 系列红线（只在 OPTIMISTIC 下赚钱）
排除。handover/E4_Fill压力测试.json。

## E1 结构引擎每日 Funnel artifact（新引擎链层）

`structure_funnel_daily.py`（抽样 467/10 股 × 近 30 bar）首跑：
13950 bar → pool 13950 → sweep 1644 → reclaim 302 → disp 238 → shift 212 → poi 212 → READY 177。
拒因全分类：BREAK_DOWN_BEFORE_SWEEP 9860（主导：多数 bar 无"破池收回"前置）、BREAK_AFTER_SWEEP 636、
INVALIDATED_BEFORE_RETEST 6、NO_RETEST_IN_WINDOW 29 —— **结构引擎层 BUY=0 可逐层解释**。
滚动 30 天历史写入 handover/structure_funnel_daily.json（自动累积基线）。

## 回归与同步

- 全量回归 **419 PASS / 0 FAIL**（新增 tests_limits 18、tests_sequence_v2 10、liquidity A1 +1）。
- 生产链未动：EVENT/CONT 口径、baseline_metrics.json 冻结值不变（E4 为研究重放，不改生产）。
- 纪律：本轮全部为**正确性/语义修复**（A 类）与**既有验证任务**（E 类），无参数寻优，无 IS→生产晋级。

## 第四轮遗留（按 F 节序号接续）

1. B1 scanner 收敛到统一 Setup Engine（下一轮主任务，含 build_seeds/stage/FVG 门替换设计）
2. E5 Parameter Surface（pivot×sweep_tol 等 4 组）
3. F7 Multi-TF Parent/Child 事件链（= V2 ITERATION 4 顺延项）
4. F8 Profile 14+ 特征补齐（扫深/位移分布/FVG·OB反应/典型持有）
5. F10 逐单 Replay 发布 Gate 工具化


## 第四轮后续：F6/B1 统一 Setup Engine SHADOW 首跑（2026-09-09）

**假设（预注册）**：旧生产硬门（R20<15 / Stage∈{UPTREND,MARKUP} / 近12bar FVG）在统一
Setup Engine（run_sequence_v2 完整链）READY 候选上，会系统性挡掉结构完整但"早期"的候选（§58 冲突）。

**结果（OOS 2025-07 起，800 股抽样，2319 个 READY 决策点）**：

| 臂 | n | OOS avg | OOS PF |
|---|---|---|---|
| 全 READY | 846 | **+2.17%** | 1.65 |
| 旧生产门通过子集 | 60 | **−0.81%** | 0.85 |

- 门通过率：R20 91.7% / **Stage 18.6%（主杀）** / FVG 77.8% / 三门全过 **8.6%**
- Δavg = +2.98pp，超过预注册判定线（1.0pp）→ **旧门误杀早期候选成立**。
- 尤其重要：旧门通过的 60 笔子集 OOS 为**负** —— 在新引擎候选分布上，旧门既漏掉 91% 样本，
  留下的部分还更差。旧门与新链语义已不匹配。

**纪律处置**：这是 SHADOW 阶段实验（只读、不动生产）。结论=统一 Setup Engine 的准入
不得沿用旧三门；新准入应基于完整链 + 结构硬门（zone 几何/RR）+ score 排序（V2 迭代 10 已证
score 仅在硬门内有效）。生产晋级需走 RESEARCH→SHADOW→PAPER→PRODUCTION（§94），
下一步为 PAPER 级连续累积。handover/B1_统一SetupEngine_SHADOW.json


## 第四轮后续：F8 Profile V2（14 特征）、F10 Replay Gate、F7 Multi-TF（进行中）

### F8 Profile V2 补齐（tests_profile 25 PASS）

新增 5 特征（全部无前视, 决策时点窗口内计算）：
- `sweep_depth_med`：窗口内 20bar 前低被破后收回的深度中位数
- `disp_mean` / `disp_q75`：位移分布均值与上四分位（core.displacement 接线）
- `fvg_reaction` / `ob_reaction`：FVG/OB 出现后 5bar 回补比例（core.fvg_ob 接线）
- `typical_hold_pct`：20bar 区间收益绝对值中位数（持有尺度代理）

### F10 逐单 Replay 发布 Gate（工具化，GATE: PASS）

`replay_gate.py` 分层对账协议：L1 订单语义 / L2 成交价（容差=2×滑点+0.3%）/ L3 规则链同族 /
L4 退出等价类。当前台账：L1 10/10、L3 10/10、L4 快照时点噪声、L2 两笔 2% 内时点差（观察清单）。
**v1 教训（诚实记录）**：曾用挂单价对账成交价、把滑点误判为执行不一致；且发现 trade_log 旧记录
entry_price 字段语义混存（信号日收盘 vs 挂单价）——已在 paper_sim 写账处统一为 limit_entry +
filled_price 双字段。Gate 判定：**PASS**（结构性 0 不一致）。`--strict` 模式供发布流水线使用。

### F7 Multi-TF Parent/Child 事件链（V2 ITERATION 4 兑现, 数据受限版）

数据现实：60m 缓存 2025-11→2026-05、15m 2026-04→2026-07，三层重叠窗仅 2026-04→05 两个月。
实验：D1 READY(Parent) × 60m 同窗低点收复(Child 确认) 对照 D1-only，预注册判定线 Δ≥1.0pp。
结果待出（本段后续补录）。


## 第四轮后续：E5 参数曲面结果 + F7 Multi-TF 判定（预注册严格执行）

### E5 Parameter Surface（pivot × sweep_tol, 16 格, 400 股 OOS 全窗）

修复 v1 bug（PIVOT_R 替换错模块导致 pivot 维度无效，诚实记录后重跑）后真曲面：

| pivot\tol | 0.3 | 0.5 | 0.8 | 1.0 |
|---|---|---|---|---|
| 2 | **+1.58**/1.45 | +0.89/1.23 | +0.83/1.23 | −0.55/0.86 |
| 3 | +1.51/1.43 | +0.87/1.23 | +0.79/1.22 | −0.62/0.84 |
| 4 | +1.44/1.41 | +0.87/1.24 | +0.68/1.19 | −0.62/0.84 |
| 5 | +1.35/1.38 | +0.85/1.23 | +0.58/1.16 | −0.53/0.86 |

- **曲面形态=平滑高原**：pivot 2~5 全部可用（最优格与邻格差 <0.7pp），tol 单调（紧确认一致更好，
  tol≥1.0 全负——扫损深度放太宽=噪声池被当流动性）。
- 尖峰（过拟合）警报：**False**（§77 理想形态："2~4 整体都可用"成立）。
- v1 教训：常量替换打到错误模块（ST.PIVOT_R 不存在于 liquidity 的局部引用），pivot 维度 16 格全同 ——
  实验代码的"参数真的生效了吗"必须先验证再谈结论。handover/E5_参数曲面.json

### F7 Multi-TF Parent/Child（预注册判定：诚实未知）

- D1 READY 347 个（重叠窗 2026-04→05），60m 同窗收复确认仅 **6 个**（<预注册样本线 30）。
- 臂A(D1-only) n=75 avg −2.21/PF0.69；臂B(60m确认) n=6 avg −5.27/PF0.38。
- **判定（按预注册线）：样本不足 → 待更长 60m/15m 缓存，结论=诚实未知**。两臂同窗皆负
  （该 2 月窗对策略整体逆风），n=6 无统计意义，不做任何方向性宣称。
- Multi-TF 角色链（§23-26）的完整验证需要 ≥1 年 60m 数据 —— 列为数据缺口，后续由
  60m/15m 缓存自然累积后重跑本脚本（已工具化）。handover/F7_MultiTF事件链.json

**证据纪律累计 8 次正确执行**（含本次 F7 拒绝在 n=6 上下结论 + E5 v1 无效实验诚实废弃重跑）。


---

# 第五轮执行报告（2026-09-10）：B4 组合引擎 / ITER12 容量 / PAPER 台账

## B4 DailyPortfolioEngine（core/portfolio.py, tests_portfolio_daily 25 PASS）

按【交易日】推进的组合回测器（旧 equity_curve 逐笔复利保留，新引擎并列）：
- 挂单撮合：限价触价 / fill_or_open 开盘兜底（事件腿回测语义对齐）/ 涨停拒买 / 停牌跳过
- 持仓退出：SL(跳空按开盘) / TP / bars_max 时间退出 / **T+1 锁定**（buy_day 当日不可卖）
- 风控：总暴露 0.95 / 单票 0.25 / 日开仓上限 / kill switch 单日 -3% 禁开仓 / **cap_shares 成交量容量截断**
- 同 code 并发仓位禁止（防覆盖丢仓 —— v1 重放 -98% 与 +1.7e6% 两个荒谬数字的共同根因之一，已修+测试锁定）

## ITER12 容量研究（蓝图 §84，三版演进，教训诚实记录）

- v1/v2 教训：①退出字段错配（sl_price 不存在→重造 SL）；②同 code 仓位覆盖；③**组合复利重放对容量问题答非所问**
  （80% 胜率+风险预算仓位+无并发上限 → INF 基线 1e26% 天文数字）—— 三次荒谬结果全部定位根因后废弃。
- v3 正确口径（成交可行性，n=1640 全量）：

| 资金 | binding@5% | @10% | @20% | 可部署比例(10%) | 判定 |
|---|---|---|---|---|---|
| 100万 | 0.12% | 0% | 0% | 100% | 完整部署 |
| 500万 | 4.02% | 0.98% | 0.18% | 99.5% | 完整部署 |
| 1000万 | 10.3% | 4.02% | 0.98% | 97.6% | **部分截断(可行)** |

**结论**：事件腿容量到 1000 万级无实质约束（10% 参与率下仅 4% 订单受量截断）；
策略瓶颈不是容量而是**信号稀缺**（1640 笔/约3年）。handover/ITER12_容量模拟.json

## Setup Engine PAPER 累积台账（B1 后续，SHADOW→PAPER 证据链）

`setup_engine_paper.py`（自动化累积器）：每日 run_sequence_v2 扫最近 bar → READY 前向记录
（zone/invalid/退出语义与 B1 SHADOW 同口径）→ 既有 OPEN 信号用 K 线推进结算（SL/TIME/INVALIDATED）。
首跑：新 READY 10 笔、台账滚动 90 天幂等。**这是统一 Setup Engine 生产晋级（§94 PAPER 级）的证据积累起点。**

## 自动化累积补丁

`funnel_history_accum.py`：selection_funnel（每日覆写）→ 追加 60 天历史（±2σ 基线前置）。
首记 2026-09-10：162 公告 → 20 正事件 → 0 单。

## 证据纪律累计 9 次

（+本次 ITER12 v1/v2 两版无效口径诚实废弃重设计 —— 荒谬数字不解释、找根因、改口径、重跑。）


---

# V3 审计响应（2026-09-10）：完成度重定义 + 立即修复清单

## 接受的重定义（逐项）
- ITERATION 0-12 = 工程任务 DONE / 研究任务含 NEGATIVE/UNKNOWN / **策略目标 NOT YET PROVEN**。
- ITER12 容量结论重写为："**历史 EVENT 样本 + 固定名义仓位假设下，1000万级尚可部署**"——
  不是"容量问题已解决"（样本仅 1640 笔历史交易、仓位公式绑定研究假设、参与率模型非盘口级）。
- 真正瓶颈重排序：**Signal Discovery（第一）** > Signal Quality > Sequence Quality > Entry Quality >
  Adaptation > Live Stability；容量后移。
- 禁止再加过滤器（162→20→0 只是一天，先累积 20~60 日 Funnel）。
- F7 验收线冻结：n<30 UNKNOWN / 30-100 PRELIMINARY / ≥100 VALIDATION / ≥200 STRONG，
  且需分年份/分 Regime/分 profile/分 TF 均有样本。
- PAPER 晋级七道门（冻结）：G1 样本≥30+≥20交易日 / G2 PF>1且avg>0 / G3 Top1<10% Top5<25% /
  G4 与回测方向一致 / G5 执行偏差受控 / G6 MDD 不超 OOS / G7 30closed只是最低门槛(目标60~100)。
- 下阶段 = V3 ALPHA VALIDATION 六阶段：A统一生产链 → B全市场60日Funnel → C Sequence Research →
  D真自适应(P(profile×regime×tf×sequence)) → E PAPER三级晋级 → F MICRO-REAL。
- 真实资金路线改为 BACKTEST→OOS→SHADOW→PAPER→**MICRO REAL**(只验真实执行)→LIMITED→FULL。

## 本轮立即修复（V3 红灯）

| 项 | 修复 | 验证 |
|---|---|---|
| §十二 死代码 | `--production` 此前未注册(getattr 永远 False) → 已注册, fail-closed 分支真实可达 | `--help` 确认 |
| §六-2 退出分叉 | **core/setup_exit.py** 单一退出实现(SL=invalid−1.5ATR / TP=3R结构位 / TIME=15bar)；PAPER 台账旧"注释TP3实际无TP"分叉消除 | tests_setup_engine 17 PASS |
| §六-1 SAMPLED 混淆 | 台账 summary.ledger_type="SAMPLED_PAPER" + sampling 说明 + setup_id/engine_version/exit_version 字段 | 台账 JSON |
| §八 Δt 日级 | `_dt()` 支持 YYYYMMDDHHMM 分钟精度(60m/15m 接入前置条件), 8位回退日级兼容 | tests_sequence 回归 |
| §十一/P0-2 | **core/setup_engine.py** 统一 Setup 对象(setup_id/sequence/POI/zone/order_type/valid_from/双版本)+validate fail-closed；SHADOW/PAPER 链已接线；**生产 scanner 切换待 PAPER 30+ closed 七道门通过后执行(不提前动生产)** | tests_setup_engine |
| §七/P0-4 | **Sequence Family Database** 五变体阶段消融(下表) | v2_seq_family_db.py |
| §五/Phase B | 结构 Funnel 升级: 每层【丢失候选前向收益 5D/10D】(回答"系统在哪里杀掉了会涨的机会") | structure_funnel_daily.py |

## Sequence Family Database 首批结果（OOS 800股, 统一退出, 预注册线判定）

| 族 | n | avg | WR | PF | 判定 |
|---|---|---|---|---|---|
| A_full 完整链 | 830 | +1.61% | 55.3% | 1.48 | 基准 |
| B_no_reclaim | 773 | +1.63% | 55.4% | 1.51 | **Δ0.02pp → RECLAIM 可简化** |
| C_trunc 前半截断 | 2606 | +0.20% | 47.2% | 1.05 | **后半结构(SHIFT/POI/RETEST)必要**(Δ1.42pp) |
| D_poi_only 纯形态 | 60639 | +0.88% | 48.4% | 1.25 | Δ-0.73pp 未达线 → 待累积 |
| E_reverse 反转 | 86 | +2.23% | 53.5% | 1.80 | n=86 → PRELIMINARY 边缘, 未知 |

已可下结论(样本足)：**收复(RECLAIM)不携带独立信息**（完整链可简化为 SWEEP→DISP→SHIFT→POI→RETEST）；
**链后半结构是 alpha 的主要来源**（截断后 edge 从 +1.61 崩至 +0.20）。
未达线者不宣称（A vs D 边缘负向、A vs E 未证）—— 留待样本累积后按同线重判。

## Funnel 丢失候选前向收益首日样本（V3 Phase B 机制落地）

L3 丢失 n=9760 fwd5 +1.91%(58.8%正) / L4 丢失 n=1131 fwd5 +1.13% —— **今日为普涨环境单日样本，
不作结论**；机制已按层固化，20~60 日累积后输出"哪一层杀掉的机会事后最值钱"。

## 待办（时间驱动）
- Funnel 60 日累积（已带丢失候选前向收益）→ Phase B 完整回答
- Sequence Family 续累积（E_reverse n=86→200+; A_vs_D 样本加深）
- PAPER 台账 30/60/100 closed 三级样本
- 60m/15m 缓存增长 → F7 重跑(验收线已冻结)
- Phase A 生产链统一：PAPER 七道门通过后实施（生产 scanner 不提前动）

**证据纪律累计 10 次**（+本轮 E_reverse/A_vs_D 拒绝在未达线上下结论）。

## V3 后续（同日）：生产链 Call-Graph 审计（§十二落地）

v3_callgraph_audit.py 逐核验点核验实际 import/调用路径（代码写了 ≠ 生产路径真的经过）：

| # | 核验点 | 状态 |
|---|---|---|
| C1 | paper_sim→core.execution.try_fill（撮合单源） | PASS |
| C2 | paper_sim→core.execution.try_exit（退出单源） | PASS |
| C3 | paper_sim→core.events.classify_title_detailed | PASS |
| C4 | paper_sim→core.structure（重复实现已消除） | PASS |
| C5 | scanner→wdh_engine.build_seeds | **TO_UNIFY**（旧语义，PAPER 七道门后切换） |
| C6 | scanner→paper_sim.stage_and_deep | **TO_UNIFY**（跨文件依赖，同上） |
| C7 | scanner --production fail-closed 可达（V3 修复） | PASS |
| C8 | 涨跌停→core（板块规则） | PASS |
| C9 | paper_sim 每日入口 --select/--monitor | PASS |
| C10 | PAPER→core.setup_exit（单源退出） | PASS |
| C11 | PAPER→core.setup_engine（统一 Setup） | PASS |

**11 项核验：9 PASS / 2 TO_UNIFY**。TO_UNIFY 的两项正是 §十一 确认的旧语义分叉——
切换条件已冻结为"PAPER 七道门通过"，不提前动生产。handover/生产链CallGraph审计.json

## V3 后续（同日）：Sequence Family Database 全市场重跑（样本敏感性如实记录）

800股首跑 vs 全市场2/3抽样重跑（同 OOS、同退出、同判定线）：

| 族 | 800股 n/avg | 全市场 n/avg | 变化 |
|---|---|---|---|
| A_full | 830 / +1.61% | 2201 / +1.41% | 稳定正 |
| B_no_reclaim | 773 / +1.63% | 2084 / +1.06% | ΔA-B 从+0.02→-0.35pp |
| C_trunc | 2606 / +0.20% | 6737 / +0.11% | 稳定无 edge（Δ≈-1.3pp） |
| D_poi_only | 60639 / +0.88% | 151791 / +0.87% | 稳定（Δ≈-0.54pp，未达1pp线） |
| E_reverse | 86 / +2.23% | 212 / +2.62% | **反转链更强** |

**关键诚实记录**：
1. **A_vs_E 翻转**：Δ 从 +0.62（未知）变为 **+1.21pp 达线 → 判定"顺序携带信息"——但方向与预期相反**：
   DISP→SHIFT→SWEEP→POI→RETEST（反转链）avg +2.62% > 完整链 +1.41%。这**不能**解读为"应该用反转链"
   （n=212 仍在 PRELIMINARY 区间且族定义不同：E 反转样本可能内嵌了更强的 sweep 深度），但**证伪了**
   "顺序只是形态标记、顺序本身无信息"的假设（§74 Ture>>Reverse 被拒绝对反转方向支持）。
2. **A_vs_B 样本敏感**：800股时 Δ+0.02pp，全市场 Δ-0.35pp——两次都在"可简化"线（±0.5pp）内，
   RECLAIM 层可简化的结论保持，但**独立信息的判定必须以全市场样本为准**，且 800 股抽样差异
   提醒所有 ≤1pp 的结论都需标注样本敏感性。
3. **稳定的结论**（两次同向）：C_trunc 无 edge（后半结构必要，Δ≈-1.3pp 稳定）；D 纯形态 POI
   弱于完整链（-0.5~-0.7pp，未达线，不宣称）；A_full 本身 OOS 稳定正（+1.4~1.6%，PF 1.39~1.48）。

**判定汇总（预注册线，全市场口径）**：
- RECLAIM 可简化：**成立**（Δ-0.35pp 在线内，n=2084 充足）
- 后半结构必要：**成立**（Δ-1.30pp，n=6737 充足）
- 顺序携带信息：**成立但方向异常**（Δ+1.21pp 达线，n=212 PRELIMINARY —— 反转链占优的机理需
  Phase C 深挖：可能是 sweep 在 POI 后出现的"回抽确认"结构，或族间样本构成差异，未证实前不采用）
- 流动性先导必要性：**UNKNOWN 维持**（Δ-0.54pp 未达线）
