# V88 专项审查：为什么生产是 V88 而不是 V517？V88 发现过什么问题？

> 日期：2026-08-17 | 证据：skills 运营文档 + 代码 + 审计 artifact + 计划文档

---

## 一、为什么是 V88 而不是 V517？—— 时代错位 + 机制残留

### 1.1 两者根本不同时代
| | V88 | V517 |
|---|---|---|
| 产生时间 | 2026-06-13（旧时代，因果重建之前） | 2026-08（新时代，因果重建流水线） |
| 来源 | V86 信号层 + V87 矩阵 → 3 年回测契约 | outcome-free seed → oracle → frozen replay |
| 生产许可 | 6-13 发布，production_gate 全 true | **V519 冻结回放失败 → V522 生产许可封锁** |
| 当前状态 | ACTIVE_VERSION 展示链仍显示它（文件存在性） | 只读研究展示 |

**V517 从未是生产** —— 它在 V519 处失败（月度门槛：2023 多个月 0 交易；2026 avg -2.32%），V522 明确 `PRODUCTION_LICENSE_BLOCKED`。所以不是"V88 vs V517 二选一"，而是"旧生产 V88 + 新研究全部未通过"。

### 1.2 "生产 V88"是机制残留，不是当前裁决
- `smc_unified.py` 的 `ACTIVE_VERSION` 链：**靠"文件存在性"选择**（`if Path('.../v88_production_report.json').exists(): ACTIVE_VERSION='V88'`）
- 计划文档（SMC_CAUSAL_PRODUCTION_REBUILD Phase 2 任务 4）**明确要求"删除报告文件存在性推断 active version 的执行语义"** —— 这个机制本身已被计划否定，但代码未改干净
- 代码自注释承认：V88 只是 **"data-routing shell"（数据路由外壳）**（`_frontend_version_label`）
- **真正的生产控制面是 registry**（smc_monitor/production_registry.json）：8-13 状态 `production_strategy=null`、`buy_enabled=false`、`FAIL_CLOSED_REPLAY_GATE_FAILED` —— **V88 的实际生产地位已被 registry 撤销**

**结论**："生产是 V88"是展示层的旧机制残留；registry 层面系统早已 EMPTY_BOOK。V88 从未被新流水线验证，也从未被正式除名 —— 它是新旧体系交替之间的历史残留。

---

## 二、V88 发现过的问题（运营文档 + 代码实证）

### 问题 1：入场价不可成交（13.7% 的交易）⚠️ 最严重
来源：`v88-live-execution-entry-contract.md`（2026-06-15）
- 用户发现：选股后无法按策略价买入，实际股价已远超历史合同价
- 审计：V88 532 笔中 **73 笔（13.7%）的 zone_limit 入场价不在 entry_date 当日 [low,high] 内**（即回测中"以这个价格成交"在现实中不可能）
- 修复：`execution_repair=ZONE_LIMIT_NOT_TOUCHED_USE_NEXT_OPEN`（改用真实 next-open 重算）
- **性质**：回测价≠可成交价，属于**不可成交/未来函数风险** —— 意味着 V88 回测收益的 13.7% 交易部分不可实现

### 问题 2：picks 污染（历史回测伪装成当前选股）
来源：`v88-current-picks-pollution-fix.md`（2026-06-14）
- `/monitor` 曾显示 **"V88 当前有效选股 884 只"**，而 V88 年回测 2026 只有 ~70 笔
- 根因：`v88_picks.json`（532 行**3 年历史回测代表**，被误标 ACTIVE_CANDIDATE）+ 旧 scanner 行混合成"当前选股"
- 修复后：`/api/picks` 只剩 6 行真实 scanner 候选
- **性质**：这就是"历史回测伪装成当前信号"的系统性问题 —— V88 是源头之一

### 问题 3：stale 候选（旧日期当当前）
来源：`v88-latest-market-day-current-picks.md`
- V88 当前选股曾显示 pick_date=20260528/29 的旧候选，用户实际无法买入
- 规则修复：超过 3 个交易日的 pick 必须 WATCH_ONLY

### 问题 4：80% 交易 1 根 K 线出场（短线捕捉，非 SMC 波段）
来源：`v92_recovery_time_stop_zone_mid_autopsy.py` 结论
- "V88 has 426/532 one-bar exits; this is a short-horizon liquidity capture"
- **V88 本质是 1 日短线流动性捕捉策略**（426/532=80% 持仓 1 根 K 线），不是传统意义上的 SMC 波段 —— 它的高胜率建立在"极短线"上

### 问题 5：前端/实盘/回测不一致（已修复的系列 bug）
来源：`v88-production-contract-release.md`（6-13 lessons）
- `smc_monitor_state.py` 的 `risk_pct<2.5%` 把 V88 全部候选 WATCH_ONLY（误拒）
- /monitor 混入 V66 旧仓位
- 数据路由与 cron 执行链脱节（`v185-cron-productionization-gap.md`：前端推广 V185 但 cron 仍跑 V88/V90/V101）

### 问题 6：未经过新流水线验证（根本问题）
- V88 是"历史回测产物"：V86 信号 + V87 矩阵 → 3 年 trades
- 从未通过 7-14 之后的"outcome-free seed → 独立 oracle → 冻结 T+1 回放 → current raw scanner 同源重建"
- 计划文档原则："历史优势不能伪装成当前信号" —— **V88 正是"历史优势"的典型**；其 picks 也无法从当前 scanner 同源重建（被 registry 的 `forbidden_fallback=true` 禁止回填）

---

## 三、为什么 700+ 次迭代没有取代 V88？

| 阶段 | 情况 |
|---|---|
| V89-V185（6-14~6-26） | gate 系列研究；V167 production_write=true、V185 指标全达标但**因果审计否决** |
| V236+（7 月） | 进入因果重建时代：每个新本体必须过"seed→oracle→冻结回放→经济门槛" |
| V297-V516 | 60m/15m 生命周期、周线 Breaker、双 SSL、行业传导等 10+ 条线 → **全部经济门禁失败** |
| V517-V525 | 量价吸收：V519 月度门槛失败 |
| V562-V625 | PIT 事件（融券/质押/增持/业绩）→ 全部 CLOSED_ECONOMIC |
| V677-V701 | 三周期状态机 / SSL-reclaim：V699 失败（WR 53.3%、2023/2026 负） |
| **结果** | **零个新本体通过生产门槛**；而 V88 作为旧产物"被保留但被降级"（registry null） |

**根因**：新旧体系标准不同 —— V88 用旧标准（精选子集 + 文件存在性）"上位"，新本体用严格标准（全样本冻结回放 + 经济门槛）全部出局。**这不是"新版本不够好"，而是"旧版本从未被同标准检验"**。当前 EMPTY_BOOK 是唯一诚实状态：既不能信任未验证的 V88，也没有通过验证的新本体。

---

## 四、结论

1. **"生产 V88"是展示层旧机制残留**（文件存在性），registry 早已将其降级为 null / EMPTY_BOOK
2. **V88 自身有 6 类已知问题**，最严重的是入场价不可成交（13.7%）和 picks 污染 —— 其回测收益部分不可实现
3. **700+ 次迭代没有取代它**，因为新标准下零晋级 —— 不是新版本差，是旧版本没被同标准检验
4. **正确的前进方向**：不再纠结 V88 vs V517，而是：① 把 V88 用新流水线重新验证（若通过则可重建为真实生产）或正式关闭；② 停止重复迭代已关闭信息族，转向新因果维度（V633 蓝图 Lane A-D）
